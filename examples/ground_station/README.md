# Satellite ground-station interfaces

This model represents the RF, baseband, flight-dynamics, and mission-control elements of a small
satellite ground station.

- `icd_csv.roc` generates an interface-control register for systems integration.
- `network_graph.roc` exports the connected architecture as Graphviz DOT.
- `operations_dashboard.roc` creates a self-contained HTML operations overview.
