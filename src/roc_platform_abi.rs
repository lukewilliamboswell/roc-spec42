//! Roc Platform ABI
//!
//! This file defines the Rust interface for hosted functions in a Roc platform.
//! It is automatically generated by the Roc glue generator.
//!
//! Hosted argument ownership:
//! - Roc transfers ownership of refcounted arguments to the hosted function.
//! - The hosted function must decref owned refcounted arguments when done.
//! - If the host stores or returns an argument, it must retain or transfer ownership explicitly.
//!
//! Import this module from the platform host and implement the listed hosted symbols
//! with the exact natural C ABI signatures shown below.

#![cfg_attr(rustfmt, rustfmt_skip)]
#![allow(dead_code)]
#![allow(improper_ctypes)]
#![allow(improper_ctypes_definitions)]
// `no_roc_std_helpers` is supplied by host build tooling and cannot be registered from source.
#![allow(unexpected_cfgs)]

use core::ffi::c_void;
use core::sync::atomic::{fence, AtomicIsize, Ordering};
#[cfg(not(no_roc_std_helpers))]
use std::alloc::Layout;

/// Runtime representation of Roc's fixed-point `Dec` value.
///
/// `num` stores the decimal value scaled by 10^18.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct RocDec {
    pub num: i128,
}

const _: [(); 16] = [(); core::mem::size_of::<RocDec>()];
const _: [(); 16] = [(); core::mem::align_of::<RocDec>()];

#[cfg(target_arch = "x86_64")]
type RocU8x16Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocI8x16Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocU16x8Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocI16x8Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocU32x4Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocI32x4Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocU64x2Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "x86_64")]
type RocI64x2Native = core::arch::x86_64::__m128i;
#[cfg(target_arch = "aarch64")]
type RocU8x16Native = core::arch::aarch64::uint8x16_t;
#[cfg(target_arch = "aarch64")]
type RocI8x16Native = core::arch::aarch64::int8x16_t;
#[cfg(target_arch = "aarch64")]
type RocU16x8Native = core::arch::aarch64::uint16x8_t;
#[cfg(target_arch = "aarch64")]
type RocI16x8Native = core::arch::aarch64::int16x8_t;
#[cfg(target_arch = "aarch64")]
type RocU32x4Native = core::arch::aarch64::uint32x4_t;
#[cfg(target_arch = "aarch64")]
type RocI32x4Native = core::arch::aarch64::int32x4_t;
#[cfg(target_arch = "aarch64")]
type RocU64x2Native = core::arch::aarch64::uint64x2_t;
#[cfg(target_arch = "aarch64")]
type RocI64x2Native = core::arch::aarch64::int64x2_t;
#[cfg(target_arch = "wasm32")]
type RocU8x16Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocI8x16Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocU16x8Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocI16x8Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocU32x4Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocI32x4Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocU64x2Native = core::arch::wasm32::v128;
#[cfg(target_arch = "wasm32")]
type RocI64x2Native = core::arch::wasm32::v128;

macro_rules! roc_simd_type {
    ($name:ident, $native:ident) => {
        #[repr(transparent)]
        #[derive(Clone, Copy)]
        pub struct $name(pub $native);
        const _: [(); 16] = [(); core::mem::size_of::<$name>()];
        const _: [(); 16] = [(); core::mem::align_of::<$name>()];
    };
}
roc_simd_type!(RocU8x16, RocU8x16Native);
roc_simd_type!(RocI8x16, RocI8x16Native);
roc_simd_type!(RocU16x8, RocU16x8Native);
roc_simd_type!(RocI16x8, RocI16x8Native);
roc_simd_type!(RocU32x4, RocU32x4Native);
roc_simd_type!(RocI32x4, RocI32x4Native);
roc_simd_type!(RocU64x2, RocU64x2Native);
roc_simd_type!(RocI64x2, RocI64x2Native);


/// Runtime representation of an opaque `Box(T)` value.
pub type RocBox = *mut c_void;

/// Host-internal allocator and diagnostic context used by helper functions in this file.
///
/// Compiled Roc code does not receive this value. The real host ABI is the set of direct
/// linker symbols declared below (`roc_alloc`, hosted symbols, and provided entrypoints).
#[repr(C)]
pub struct RocHost {
    pub env: *mut c_void,
    pub roc_alloc: extern "C" fn(*mut RocHost, usize, usize) -> *mut c_void,
    pub roc_dealloc: extern "C" fn(*mut RocHost, *mut c_void, usize),
    pub roc_realloc: extern "C" fn(*mut RocHost, *mut c_void, usize, usize) -> *mut c_void,
    pub roc_dbg: extern "C" fn(*mut RocHost, *const u8, usize),
    pub roc_expect_failed: extern "C" fn(*mut RocHost, *const u8, usize),
    pub roc_crashed: extern "C" fn(*mut RocHost, *const u8, usize),
}

impl RocHost {
    /// Allocate memory with the given alignment and length.
    ///
    /// # Safety
    /// The returned pointer must be used only according to Roc allocation layout
    /// rules and later released through the matching host deallocator.
    #[inline]
    pub unsafe fn alloc(&self, alignment: usize, length: usize) -> *mut c_void {
        let host = self as *const RocHost as *mut RocHost;
        (self.roc_alloc)(host, length, alignment)
    }

    /// Deallocate memory previously allocated with `alloc`.
    ///
    /// # Safety
    /// `ptr` must have been allocated by this host with the same alignment and must
    /// not be used after this call.
    #[inline]
    pub unsafe fn dealloc(&self, ptr: *mut c_void, alignment: usize) {
        let host = self as *const RocHost as *mut RocHost;
        (self.roc_dealloc)(host, ptr, alignment);
    }

    /// Reallocate memory to a new size.
    ///
    /// # Safety
    /// `old_ptr` must have been allocated by this host with the same alignment.
    /// The returned pointer replaces `old_ptr`; the old pointer must not be used.
    #[inline]
    pub unsafe fn realloc(
        &self,
        old_ptr: *mut c_void,
        alignment: usize,
        new_length: usize,
    ) -> *mut c_void {
        let host = self as *const RocHost as *mut RocHost;
        (self.roc_realloc)(host, old_ptr, new_length, alignment)
    }
}

#[inline]
fn checked_add_usize(left: usize, right: usize, context: &str) -> usize {
    left.checked_add(right).expect(context)
}

#[inline]
fn checked_mul_usize(left: usize, right: usize, context: &str) -> usize {
    left.checked_mul(right).expect(context)
}

#[inline]
fn shifted_capacity(capacity: usize) -> usize {
    capacity.checked_shl(1).expect("Roc capacity does not fit shifted representation")
}

/// Uniform ABI function pointer stored in `RocErasedCallablePayload`.
///
/// The final `reuse` pointer is nullable. Non-null must be the callable data
/// pointer whose inline capture begins at `capture`; it transfers one owned
/// reference to the callee. The caller must not use or decref that ownership
/// unit after the call. The callee consumes it exactly once, whether or not the
/// result can reuse the allocation.
pub type RocErasedCallableFn = extern "C" fn(*mut RocHost, *mut u8, *const u8, *mut u8, *mut u8, *mut *const c_void);

/// Final-drop callback for inline erased-callable captures.
pub type RocErasedCallableOnDrop = extern "C" fn(*mut u8, *mut RocHost);

/// Payload header for `Box(function)`.
#[repr(C)]
#[derive(Debug, Clone, Copy)]
pub struct RocErasedCallablePayload {
    pub callable_fn_ptr: RocErasedCallableFn,
    pub on_drop: Option<RocErasedCallableOnDrop>,
}

const _: () = assert!(core::mem::size_of::<RocErasedCallablePayload>() == 2 * core::mem::size_of::<usize>(), "RocErasedCallablePayload size mismatch");
const _: () = assert!(core::mem::offset_of!(RocErasedCallablePayload, callable_fn_ptr) == 0, "RocErasedCallablePayload.callable_fn_ptr offset mismatch");
const _: () = assert!(core::mem::offset_of!(RocErasedCallablePayload, on_drop) == core::mem::size_of::<usize>(), "RocErasedCallablePayload.on_drop offset mismatch");

/// Runtime representation of `Box(function)`.
pub type RocErasedCallable = *mut u8;

pub const ROC_ERASED_CALLABLE_CAPTURE_ALIGNMENT: usize = 16;
pub const ROC_ERASED_CALLABLE_PAYLOAD_ALIGNMENT: usize = 16;
pub const ROC_ERASED_CALLABLE_CAPTURE_OFFSET: usize =
    (core::mem::size_of::<RocErasedCallablePayload>() + 15) & !15;

#[inline]
pub fn roc_erased_callable_payload_size(capture_size: usize) -> usize {
    checked_add_usize(
        ROC_ERASED_CALLABLE_CAPTURE_OFFSET,
        capture_size,
        "Roc erased-callable payload size overflow",
    )
}

#[inline]
/// # Safety
/// `callable` must be a non-null Roc erased-callable data pointer.
pub unsafe fn roc_erased_callable_payload_ptr(callable: RocErasedCallable) -> *mut RocErasedCallablePayload {
    callable as *mut RocErasedCallablePayload
}

#[inline]
/// # Safety
/// `callable` must be a non-null Roc erased-callable data pointer.
pub unsafe fn roc_erased_callable_capture_ptr(callable: RocErasedCallable) -> *mut u8 {
    callable.add(ROC_ERASED_CALLABLE_CAPTURE_OFFSET)
}

/// Allocate a Roc erased callable payload.
///
/// # Safety
/// The caller must initialize and use the returned callable according to Roc's
/// erased-callable ABI. `callable_fn_ptr` and `on_drop` must have matching ABI
/// signatures for the captured payload.
pub unsafe fn roc_erased_callable_allocate(
    roc_host: &RocHost,
    callable_fn_ptr: RocErasedCallableFn,
    on_drop: Option<RocErasedCallableOnDrop>,
    capture_size: usize,
) -> RocErasedCallable {
    let ptr_width = core::mem::size_of::<usize>();
    let alignment = core::cmp::max(ptr_width, ROC_ERASED_CALLABLE_PAYLOAD_ALIGNMENT);
    let extra_bytes = core::cmp::max(ptr_width, ROC_ERASED_CALLABLE_PAYLOAD_ALIGNMENT);
    let total = checked_add_usize(
        extra_bytes,
        roc_erased_callable_payload_size(capture_size),
        "Roc erased-callable allocation size overflow",
    );
    let base = roc_host.alloc(alignment, total) as *mut u8;
    let data = base.add(extra_bytes);
    let rc = data.sub(core::mem::size_of::<isize>()) as *mut isize;
    *rc = 1;
    let payload = roc_erased_callable_payload_ptr(data);
    *payload = RocErasedCallablePayload { callable_fn_ptr, on_drop };
    data
}

/// Payload drop callback for a boxed value.
///
/// The callback receives the boxed payload data pointer and must recursively
/// decref any Roc refcounted values inside the payload. It must not free the
/// box allocation; `decref_box_with` and `free_box_with` free it after the callback.
pub type RocBoxPayloadDecref = extern "C" fn(*mut c_void, *mut RocHost);

/// Increment the refcount of a boxed payload data pointer.
///
/// # Safety
/// `data_ptr` must be a valid Roc box payload pointer. The caller must ensure
/// that the extra retained references are balanced by later decrefs.
pub unsafe fn incref_box(data_ptr: RocBox, amount: isize) {
    let data = match box_data_ptr(data_ptr) {
        Some(ptr) => ptr,
        None => return,
    };
    let rc = box_refcount_ptr(data);
    if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
        return; // REFCOUNT_STATIC_DATA
    }
    unsafe {
        (*rc).fetch_add(amount, Ordering::Relaxed);
    }
}

/// Allocate a Roc box and return a pointer to its payload data.
///
/// # Safety
/// The returned payload memory is uninitialized. The caller must initialize it
/// according to the Roc type before exposing it to safe APIs or Roc code.
pub unsafe fn allocate_box(
    payload_size: usize,
    payload_alignment: usize,
    payload_contains_refcounted: bool,
    roc_host: &RocHost,
) -> RocBox {
    let ptr_width = core::mem::size_of::<usize>();
    let required_space = if payload_contains_refcounted { checked_mul_usize(2, ptr_width, "Roc box header size overflow") } else { ptr_width };
    let header_bytes = required_space.max(payload_alignment);
    let alloc_alignment = ptr_width.max(payload_alignment);
    let total = checked_add_usize(header_bytes, payload_size, "Roc box allocation size overflow");
    let base = roc_host.alloc(alloc_alignment, total) as *mut u8;
    let data = base.add(header_bytes);
    unsafe {
        let rc = data.sub(core::mem::size_of::<isize>()) as *mut isize;
        *rc = 1;
    }
    data as RocBox
}

/// Decrement a pointer-aligned boxed payload with no Roc refcounted values.
///
/// # Safety
/// `data_ptr` must be a valid Roc box payload pointer owned by this reference.
pub unsafe fn decref_box(data_ptr: RocBox, roc_host: &RocHost) {
    unsafe {
        decref_box_with(data_ptr, core::mem::align_of::<usize>(), false, None, roc_host);
    }
}

/// Increment a boxed function closure.
///
/// # Safety
/// `callable` must be a valid Roc erased-callable payload pointer.
pub unsafe fn incref_erased_callable(callable: RocErasedCallable, amount: isize) {
    unsafe {
        incref_box(callable as RocBox, amount);
    }
}

/// Decrement a boxed function closure and run its capture drop callback on final release.
///
/// # Safety
/// `callable` must be a valid Roc erased-callable payload pointer owned by this reference.
pub unsafe fn decref_erased_callable(callable: RocErasedCallable, roc_host: &RocHost) {
    unsafe {
        decref_box_with(
            callable as RocBox,
            ROC_ERASED_CALLABLE_PAYLOAD_ALIGNMENT,
            false,
            Some(drop_erased_callable_payload),
            roc_host,
        );
    }
}

extern "C" fn drop_erased_callable_payload(data_ptr: *mut c_void, roc_host: *mut RocHost) {
    if data_ptr.is_null() || roc_host.is_null() {
        return;
    }
    unsafe {
        let callable = data_ptr as RocErasedCallable;
        let payload = roc_erased_callable_payload_ptr(callable);
        if let Some(on_drop) = (*payload).on_drop {
            on_drop(roc_erased_callable_capture_ptr(callable), roc_host);
        }
    }
}

/// Decrement a boxed payload and run payload teardown when this is the final ref.
///
/// `payload_contains_refcounted` must match the value passed to `allocate_box`:
/// it determines the box header size, and is independent of whether a
/// `payload_decref` teardown callback is supplied. A host resource handle such
/// as `Box(U64)` holding a raw pointer has `payload_contains_refcounted: false`
/// even when it provides a teardown callback to free the underlying resource.
///
/// # Safety
/// `data_ptr` must be a valid Roc box payload pointer owned by this reference,
/// and `payload_alignment`/`payload_contains_refcounted` must match allocation.
pub unsafe fn decref_box_with(
    data_ptr: RocBox,
    payload_alignment: usize,
    payload_contains_refcounted: bool,
    payload_decref: Option<RocBoxPayloadDecref>,
    roc_host: &RocHost,
) {
    let data = match box_data_ptr(data_ptr) {
        Some(ptr) => ptr,
        None => return,
    };
    let rc = box_refcount_ptr(data);
    if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
        return; // REFCOUNT_STATIC_DATA
    }
    let prev = unsafe { (*rc).fetch_sub(1, Ordering::Release) };
    if prev == 1 {
        fence(Ordering::Acquire);
        if let Some(callback) = payload_decref {
            callback(data_ptr, roc_host as *const RocHost as *mut RocHost);
        }
        free_box_allocation(data, payload_alignment, payload_contains_refcounted, roc_host);
    }
}

/// Free a boxed payload allocation immediately after running payload teardown.
///
/// See `decref_box_with` for the meaning of `payload_contains_refcounted`.
///
/// # Safety
/// `data_ptr` must be a valid Roc box payload pointer that will not be used after this call.
pub unsafe fn free_box_with(
    data_ptr: RocBox,
    payload_alignment: usize,
    payload_contains_refcounted: bool,
    payload_decref: Option<RocBoxPayloadDecref>,
    roc_host: &RocHost,
) {
    let data = match box_data_ptr(data_ptr) {
        Some(ptr) => ptr,
        None => return,
    };
    if let Some(callback) = payload_decref {
        callback(data_ptr, roc_host as *const RocHost as *mut RocHost);
    }
    free_box_allocation(data, payload_alignment, payload_contains_refcounted, roc_host);
}

/// Return true when a boxed payload data pointer has exactly one live ref.
pub fn is_unique_box(data_ptr: RocBox) -> bool {
    let data = match box_data_ptr(data_ptr) {
        Some(ptr) => ptr,
        None => return true,
    };
    let rc = box_refcount_ptr(data);
    unsafe { (*rc).load(Ordering::Acquire) == 1 }
}

fn box_data_ptr(data_ptr: RocBox) -> Option<*mut u8> {
    if data_ptr.is_null() {
        None
    } else {
        Some(data_ptr as *mut u8)
    }
}

fn box_refcount_ptr(data: *mut u8) -> *mut AtomicIsize {
    unsafe { data.sub(core::mem::size_of::<isize>()) as *mut AtomicIsize }
}

fn free_box_allocation(
    data: *mut u8,
    payload_alignment: usize,
    payload_contains_refcounted: bool,
    roc_host: &RocHost,
) {
    let ptr_width = core::mem::size_of::<usize>();
    let required_space = if payload_contains_refcounted { checked_mul_usize(2, ptr_width, "Roc box header size overflow") } else { ptr_width };
    let header_bytes = required_space.max(payload_alignment);
    let alloc_alignment = ptr_width.max(payload_alignment);
    let base = unsafe { data.sub(header_bytes) } as *mut c_void;
    unsafe {
        roc_host.dealloc(base, alloc_alignment);
    }
}

/// A Roc string value. Small strings (up to 23 bytes on 64-bit) are stored inline;
/// larger strings are heap-allocated with a reference count.
///
/// `bytes` is never tagged. Operations, host code, glue code, and object-file
/// relocations can use it directly as the UTF-8 byte pointer for non-small
/// strings. Seamless-slice tagging lives in `capacity_or_alloc_ptr` instead.
/// Big-string capacity is stored shifted left by one bit, so max capacity is
/// essentially `isize::MAX` bytes: about 2 GiB on 32-bit targets and 8 EiB on
/// 64-bit targets.
///
/// This type is ABI-compatible with the Zig RocStr (24 bytes, `#[repr(C)]`).
#[repr(C)]
#[derive(Clone, Copy)]
pub struct RocStr {
    pub bytes: *mut u8,
    pub capacity_or_alloc_ptr: usize,
    pub length: usize,
}

const _: () = assert!(core::mem::size_of::<RocStr>() == 3 * core::mem::size_of::<usize>(), "RocStr size mismatch");
const _: () = assert!(core::mem::align_of::<RocStr>() == core::mem::align_of::<usize>(), "RocStr alignment mismatch");
const _: () = assert!(core::mem::offset_of!(RocStr, bytes) == 0, "RocStr.bytes offset mismatch");
const _: () = assert!(core::mem::offset_of!(RocStr, capacity_or_alloc_ptr) == core::mem::size_of::<usize>(), "RocStr.capacity_or_alloc_ptr offset mismatch");
const _: () = assert!(core::mem::offset_of!(RocStr, length) == 2 * core::mem::size_of::<usize>(), "RocStr.length offset mismatch");

const ROC_STR_SIZE: usize = core::mem::size_of::<RocStr>();
const ROC_SMALL_STR_MAX_LEN: usize = ROC_STR_SIZE - 1;
const ROC_SMALL_STR_BIT: usize = isize::MIN as usize;
const ROC_SEAMLESS_SLICE_TAG: usize = 1;

impl RocStr {
    /// Return an empty RocStr (small string with zero length).
    pub fn empty() -> Self {
        Self {
            bytes: core::ptr::null_mut(),
            capacity_or_alloc_ptr: 0,
            length: ROC_SMALL_STR_BIT,
        }
    }

    /// Return true if this string is stored inline (small string optimization).
    #[inline]
    pub fn is_small_str(&self) -> bool {
        (self.length as isize) < 0
    }

    /// Return true if this string is a seamless slice into another allocation.
    #[inline]
    pub fn is_seamless_slice(&self) -> bool {
        !self.is_small_str() && (self.capacity_or_alloc_ptr & ROC_SEAMLESS_SLICE_TAG) != 0
    }

    /// Return the length of the string in bytes.
    #[inline]
    pub fn len(&self) -> usize {
        if self.is_small_str() {
            let bytes_ptr = self as *const Self as *const u8;
            let last_byte = unsafe { *bytes_ptr.add(ROC_STR_SIZE - 1) };
            (last_byte ^ 0b1000_0000) as usize
        } else {
            self.length
        }
    }

    /// Return true if the string has zero length.
    #[inline]
    pub fn is_empty(&self) -> bool {
        self.len() == 0
    }

    /// Return the string contents as a byte slice.
    pub fn as_slice(&self) -> &[u8] {
        let ptr = self.as_u8_ptr();
        unsafe { core::slice::from_raw_parts(ptr, self.len()) }
    }

    /// Return a pointer to the raw UTF-8 bytes.
    #[inline]
    pub fn as_u8_ptr(&self) -> *const u8 {
        if self.is_small_str() {
            self as *const Self as *const u8
        } else {
            self.bytes as *const u8
        }
    }

    /// Return the string contents as a `&str`.
    pub fn as_str(&self) -> &str {
        core::str::from_utf8(self.as_slice()).expect("RocStr contained invalid UTF-8")
    }

    /// Create a RocStr from a UTF-8 byte slice, using `roc_host` for heap allocation if needed.
    pub fn from_slice(slice: &[u8], roc_host: &RocHost) -> Self {
        core::str::from_utf8(slice).expect("RocStr::from_slice requires valid UTF-8");
        if slice.len() < ROC_STR_SIZE {
            let mut result = Self::empty();
            let ptr = &mut result as *mut Self as *mut u8;
            unsafe {
                core::ptr::copy_nonoverlapping(slice.as_ptr(), ptr, slice.len());
                *ptr.add(ROC_STR_SIZE - 1) = (slice.len() as u8) | 0b1000_0000;
            }
            result
        } else {
            let ptr_width = core::mem::size_of::<usize>();
            let total = checked_add_usize(ptr_width, slice.len(), "RocStr allocation size overflow");
            let base = unsafe { roc_host.alloc(core::mem::align_of::<usize>(), total) };
            let data_ptr = unsafe { (base as *mut u8).add(ptr_width) };
            // Write refcount = 1
            unsafe {
                let rc = (data_ptr as *mut isize).sub(1);
                *rc = 1;
                core::ptr::copy_nonoverlapping(slice.as_ptr(), data_ptr, slice.len());
            }
            Self {
                bytes: data_ptr,
                capacity_or_alloc_ptr: shifted_capacity(slice.len()),
                length: slice.len(),
            }
        }
    }

    /// Create a RocStr from a `&str`.
    pub fn from_str(s: &str, roc_host: &RocHost) -> Self {
        Self::from_slice(s.as_bytes(), roc_host)
    }

    /// Decrement the reference count; frees the allocation when it reaches zero.
    ///
    /// # Safety
    /// `self` must own one live Roc reference. Calling this more than once for the
    /// same ownership reference may double-free.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        if self.is_small_str() {
            return;
        }
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return;
        }
        let rc = unsafe { (alloc_ptr as *mut AtomicIsize).sub(1) };
        if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
            return; // REFCOUNT_STATIC_DATA—bytes are in read-only memory
        }
        let prev = unsafe { (*rc).fetch_sub(1, Ordering::Release) };
        if prev == 1 {
            fence(Ordering::Acquire);
            let ptr_width = core::mem::size_of::<usize>();
            let base = unsafe { alloc_ptr.sub(ptr_width) } as *mut c_void;
            unsafe {
                roc_host.dealloc(base, core::mem::align_of::<usize>());
            }
        }
    }

    /// Increment the reference count by `amount`.
    ///
    /// # Safety
    /// `self` must point at a live Roc allocation. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        if self.is_small_str() {
            return;
        }
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return;
        }
        let rc = unsafe { (alloc_ptr as *mut AtomicIsize).sub(1) };
        if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
            return; // REFCOUNT_STATIC_DATA
        }
        unsafe {
            (*rc).fetch_add(amount, Ordering::Relaxed);
        }
    }

    /// Return true if this string has a reference count of exactly one.
    pub fn is_unique(&self) -> bool {
        if self.is_small_str() {
            return true;
        }
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return true;
        }
        unsafe {
            let rc = (alloc_ptr as *const AtomicIsize).sub(1);
            let count = (*rc).load(Ordering::Acquire);
            count == 0 || count == 1
        }
    }

    fn get_allocation_ptr(&self) -> *mut u8 {
        if self.is_seamless_slice() {
            (self.capacity_or_alloc_ptr & !ROC_SEAMLESS_SLICE_TAG) as *mut u8
        } else {
            self.bytes
        }
    }
}

impl core::fmt::Debug for RocStr {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_struct("RocStr")
            .field("len", &self.len())
            .field("is_small", &self.is_small_str())
            .finish()
    }
}

/// A generic Roc list. Elements are reference-counted and heap-allocated.
///
/// When `ELEMENTS_REFCOUNTED` is true (the default via `RocList<T>`), an extra
/// `ptr_width` bytes are reserved in the allocation header for the element count,
/// matching the Roc runtime's `allocateWithRefcount` layout.
pub type RocList<T> = RocListWith<T, true>;

/// Parameterized list constructor; use `RocList<T>` for refcounted elements.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct RocListWith<T, const ELEMENTS_REFCOUNTED: bool> {
    pub elements: *mut T,
    pub length: usize,
    pub capacity_or_alloc_ptr: usize,
}

const _: () = assert!(core::mem::size_of::<RocList<u8>>() == 3 * core::mem::size_of::<usize>(), "RocList size mismatch");
const _: () = assert!(core::mem::align_of::<RocList<u8>>() == core::mem::align_of::<usize>(), "RocList alignment mismatch");
const _: () = assert!(core::mem::offset_of!(RocList<u8>, elements) == 0, "RocList.elements offset mismatch");
const _: () = assert!(core::mem::offset_of!(RocList<u8>, length) == core::mem::size_of::<usize>(), "RocList.length offset mismatch");
const _: () = assert!(core::mem::offset_of!(RocList<u8>, capacity_or_alloc_ptr) == 2 * core::mem::size_of::<usize>(), "RocList.capacity_or_alloc_ptr offset mismatch");

impl<T, const ELEMENTS_REFCOUNTED: bool> RocListWith<T, ELEMENTS_REFCOUNTED> {
    #[inline]
    fn header_bytes() -> usize {
        let ptr_width = core::mem::size_of::<usize>();
        let required_space = if ELEMENTS_REFCOUNTED { checked_mul_usize(2, ptr_width, "RocList header size overflow") } else { ptr_width };
        required_space.max(core::mem::align_of::<T>())
    }

    /// Return an empty RocList.
    pub fn empty() -> Self {
        Self {
            elements: core::ptr::null_mut(),
            length: 0,
            capacity_or_alloc_ptr: 0,
        }
    }

    /// Return the number of elements in the list.
    #[inline]
    pub fn len(&self) -> usize {
        self.length
    }

    /// Return true if the list has zero elements.
    #[inline]
    pub fn is_empty(&self) -> bool {
        self.length == 0
    }

    /// Return true if this list is a seamless slice into another allocation.
    /// Slices share the rc slot with their backing allocation; the alloc ptr is
    /// encoded in `capacity_or_alloc_ptr` with the low bit set.
    #[inline]
    pub fn is_seamless_slice(&self) -> bool {
        (self.capacity_or_alloc_ptr & 1) != 0
    }

    /// Resolve `self` to the start of its backing allocation (the element block
    /// just after the rc slot). Returns `null` for empty lists. Handles both
    /// whole-backing and seamless-slice forms.
    fn get_allocation_ptr(&self) -> *mut u8 {
        if self.is_seamless_slice() {
            (self.capacity_or_alloc_ptr & !1) as *mut u8
        } else {
            self.elements as *mut u8
        }
    }

    fn allocation_element_count(&self) -> usize {
        if self.is_seamless_slice() && ELEMENTS_REFCOUNTED {
            let alloc_ptr = self.get_allocation_ptr();
            if alloc_ptr.is_null() {
                return 0;
            }
            unsafe {
                let ptr = alloc_ptr as *const usize;
                *ptr.sub(2)
            }
        } else {
            self.length
        }
    }

    /// Return the list elements as a slice.
    pub fn as_slice(&self) -> &[T] {
        if self.elements.is_null() {
            &[]
        } else {
            unsafe { core::slice::from_raw_parts(self.elements, self.length) }
        }
    }

    /// Return all items in the backing allocation, not just this slice.
    pub fn allocation_items(&self) -> &[T] {
        if self.elements.is_null() {
            &[]
        } else {
            unsafe { core::slice::from_raw_parts(self.get_allocation_ptr() as *const T, self.allocation_element_count()) }
        }
    }

    /// Allocate a new list with space for `length` elements.
    ///
    /// # Safety
    /// The returned element memory is uninitialized while `length` is already
    /// set. The caller must initialize every element before exposing the list
    /// to safe APIs, Roc code, or generated refcount helpers.
    pub unsafe fn allocate(length: usize, roc_host: &RocHost) -> Self {
        if length == 0 {
            return Self::empty();
        }
        let align = core::mem::align_of::<T>().max(core::mem::align_of::<usize>());
        let header_bytes = Self::header_bytes();
        let data_bytes = checked_mul_usize(length, core::mem::size_of::<T>(), "RocList allocation element bytes overflow");
        let total = checked_add_usize(data_bytes, header_bytes, "RocList allocation size overflow");
        let base = roc_host.alloc(align, total);
        let data_ptr = (base as *mut u8).add(header_bytes);
        unsafe {
            let rc = (data_ptr as *mut isize).sub(1);
            *rc = 1;
            if ELEMENTS_REFCOUNTED {
                let count = (data_ptr as *mut usize).sub(2);
                *count = length;
            }
        }
        Self {
            elements: data_ptr as *mut T,
            length,
            capacity_or_alloc_ptr: shifted_capacity(length),
        }
    }

    /// Create a RocList from a slice, copying elements into a new allocation.
    ///
    /// # Safety
    /// This is a shallow copy. For element types that own Roc references, the
    /// caller must ensure each copied element is already retained for the new
    /// list or will not be decref'd through another owner.
    pub unsafe fn from_slice(slice: &[T], roc_host: &RocHost) -> Self where T: Copy {
        if slice.is_empty() {
            return Self::empty();
        }
        let list = unsafe { Self::allocate(slice.len(), roc_host) };
        unsafe {
            core::ptr::copy_nonoverlapping(
                slice.as_ptr(),
                list.elements,
                slice.len(),
            );
        }
        list
    }

    /// Decrement the reference count; frees the allocation when it reaches zero.
    ///
    /// # Safety
    /// `self` must own one live Roc list reference. Calling this more than once
    /// for the same ownership reference may double-free.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        if self.elements.is_null() {
            return;
        }
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return;
        }
        let align = core::mem::align_of::<T>().max(core::mem::align_of::<usize>());
        let header_bytes = Self::header_bytes();
        let rc = unsafe { (alloc_ptr as *mut AtomicIsize).sub(1) };
        if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
            return; // REFCOUNT_STATIC_DATA—elements are in read-only memory
        }
        let prev = unsafe { (*rc).fetch_sub(1, Ordering::Release) };
        if prev == 1 {
            fence(Ordering::Acquire);
            let base = unsafe { alloc_ptr.sub(header_bytes) } as *mut c_void;
            unsafe {
                roc_host.dealloc(base, align);
            }
        }
    }

    /// Increment the reference count by `amount`.
    ///
    /// # Safety
    /// `self` must point at a live Roc list allocation. The retained references
    /// must be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        if self.elements.is_null() {
            return;
        }
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return;
        }
        let rc = unsafe { (alloc_ptr as *mut AtomicIsize).sub(1) };
        if unsafe { (*rc).load(Ordering::Relaxed) } == 0 {
            return; // REFCOUNT_STATIC_DATA
        }
        unsafe {
            (*rc).fetch_add(amount, Ordering::Relaxed);
        }
    }

    /// Return true if this list has a reference count of exactly one.
    pub fn is_unique(&self) -> bool {
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return true;
        }
        unsafe {
            let rc = (alloc_ptr as *const AtomicIsize).sub(1);
            let count = (*rc).load(Ordering::Acquire);
            count == 0 || count == 1
        }
    }

    /// Return true if this list's allocation has exactly one counted ref.
    pub fn has_one_ref(&self) -> bool {
        let alloc_ptr = self.get_allocation_ptr();
        if alloc_ptr.is_null() {
            return false;
        }
        unsafe {
            let rc = (alloc_ptr as *const AtomicIsize).sub(1);
            (*rc).load(Ordering::Acquire) == 1
        }
    }
}

impl<T: core::fmt::Debug, const ELEMENTS_REFCOUNTED: bool> core::fmt::Debug for RocListWith<T, ELEMENTS_REFCOUNTED> {
    fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result {
        f.debug_list().entries(self.as_slice().iter()).finish()
    }
}

/// Element type for __AnonStruct_5f154a81aee47322
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct5f154a81aee47322 {
    pub model_digest: RocStr,
    pub semantic_api_version: RocStr,
    pub spec42_version: RocStr,
}

/// Element type for __AnonStruct_5f154a81aee47322
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct5f154a81aee47322 {
    pub model_digest: RocStr,
    pub semantic_api_version: RocStr,
    pub spec42_version: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct5f154a81aee47322>() == 72, "AnonStruct5f154a81aee47322 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct5f154a81aee47322>() == 8, "AnonStruct5f154a81aee47322 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct5f154a81aee47322>() == 36, "AnonStruct5f154a81aee47322 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct5f154a81aee47322>() == 4, "AnonStruct5f154a81aee47322 alignment mismatch");

/// Element type for __AnonStruct_d19c6c897d0b2f23
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructD19c6c897d0b2f23 {
    pub handle: RocStr,
    pub metaclass: RocStr,
    pub name: NoneOrSomeType11,
    pub qualified_name: RocStr,
    pub semantic_id: RocStr,
    pub library_element: bool,
}

/// Element type for __AnonStruct_d19c6c897d0b2f23
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructD19c6c897d0b2f23 {
    pub handle: RocStr,
    pub metaclass: RocStr,
    pub name: NoneOrSomeType11,
    pub qualified_name: RocStr,
    pub semantic_id: RocStr,
    pub library_element: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructD19c6c897d0b2f23>() == 136, "AnonStructD19c6c897d0b2f23 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructD19c6c897d0b2f23>() == 8, "AnonStructD19c6c897d0b2f23 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructD19c6c897d0b2f23>() == 68, "AnonStructD19c6c897d0b2f23 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructD19c6c897d0b2f23>() == 4, "AnonStructD19c6c897d0b2f23 alignment mismatch");

/// Element type for __AnonStruct_1ebccf7a1ff71691
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct1ebccf7a1ff71691 {
    pub declared_name: NoneOrSomeType18,
    pub direction: NoneOrSomeType18,
    pub documentation: NoneOrSomeType18,
    pub effective_name: NoneOrSomeType18,
    pub evaluated_value: NoneOrSomeType18,
    pub multiplicity: NoneOrSomeType19,
    pub owner: NoneOrSomeType21,
    pub short_name: NoneOrSomeType18,
    pub source_uri: RocStr,
    pub summary: AnonStructD19c6c897d0b2f23,
    pub source_range: AnonStructF854c29f636ef8f8,
    pub abstract_flag: bool,
    pub composite: NoneOrSomeType17,
    pub conjugated: bool,
    pub constant: bool,
    pub definition: bool,
    pub derived: bool,
    pub end: bool,
    pub individual: bool,
    pub ordered: NoneOrSomeType17,
    pub reference: NoneOrSomeType17,
    pub unique: NoneOrSomeType17,
    pub variation: bool,
}

/// Element type for __AnonStruct_1ebccf7a1ff71691
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct1ebccf7a1ff71691 {
    pub declared_name: NoneOrSomeType18,
    pub direction: NoneOrSomeType18,
    pub documentation: NoneOrSomeType18,
    pub effective_name: NoneOrSomeType18,
    pub evaluated_value: NoneOrSomeType18,
    pub multiplicity: NoneOrSomeType19,
    pub owner: NoneOrSomeType21,
    pub short_name: NoneOrSomeType18,
    pub source_uri: RocStr,
    pub summary: AnonStructD19c6c897d0b2f23,
    pub source_range: AnonStructF854c29f636ef8f8,
    pub abstract_flag: bool,
    pub composite: NoneOrSomeType17,
    pub conjugated: bool,
    pub constant: bool,
    pub definition: bool,
    pub derived: bool,
    pub end: bool,
    pub individual: bool,
    pub ordered: NoneOrSomeType17,
    pub reference: NoneOrSomeType17,
    pub unique: NoneOrSomeType17,
    pub variation: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct1ebccf7a1ff71691>() == 608, "AnonStruct1ebccf7a1ff71691 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct1ebccf7a1ff71691>() == 8, "AnonStruct1ebccf7a1ff71691 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct1ebccf7a1ff71691>() == 320, "AnonStruct1ebccf7a1ff71691 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct1ebccf7a1ff71691>() == 4, "AnonStruct1ebccf7a1ff71691 alignment mismatch");

/// Element type for __AnonStruct_e7b668836e719b96
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructE7b668836e719b96 {
    pub lower: NoneOrSomeType18,
    pub upper: NoneOrSomeType18,
    pub implied: bool,
    pub ordered: bool,
    pub unique: NoneOrSomeType17,
}

/// Element type for __AnonStruct_e7b668836e719b96
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructE7b668836e719b96 {
    pub lower: NoneOrSomeType18,
    pub upper: NoneOrSomeType18,
    pub implied: bool,
    pub ordered: bool,
    pub unique: NoneOrSomeType17,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructE7b668836e719b96>() == 72, "AnonStructE7b668836e719b96 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructE7b668836e719b96>() == 8, "AnonStructE7b668836e719b96 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructE7b668836e719b96>() == 36, "AnonStructE7b668836e719b96 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructE7b668836e719b96>() == 4, "AnonStructE7b668836e719b96 alignment mismatch");

/// Element type for __AnonStruct_f854c29f636ef8f8
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructF854c29f636ef8f8 {
    pub end_character: u32,
    pub end_line: u32,
    pub start_character: u32,
    pub start_line: u32,
}

/// Element type for __AnonStruct_f854c29f636ef8f8
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructF854c29f636ef8f8 {
    pub end_character: u32,
    pub end_line: u32,
    pub start_character: u32,
    pub start_line: u32,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructF854c29f636ef8f8>() == 16, "AnonStructF854c29f636ef8f8 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructF854c29f636ef8f8>() == 4, "AnonStructF854c29f636ef8f8 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructF854c29f636ef8f8>() == 16, "AnonStructF854c29f636ef8f8 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructF854c29f636ef8f8>() == 4, "AnonStructF854c29f636ef8f8 alignment mismatch");

/// Element type for __AnonStruct_682d6d8805bd09e9
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct682d6d8805bd09e9 {
    pub kind: RocStr,
    pub source: AnonStructD19c6c897d0b2f23,
    pub target: AnonStructD19c6c897d0b2f23,
    pub implied: bool,
}

/// Element type for __AnonStruct_682d6d8805bd09e9
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct682d6d8805bd09e9 {
    pub kind: RocStr,
    pub source: AnonStructD19c6c897d0b2f23,
    pub target: AnonStructD19c6c897d0b2f23,
    pub implied: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct682d6d8805bd09e9>() == 304, "AnonStruct682d6d8805bd09e9 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct682d6d8805bd09e9>() == 8, "AnonStruct682d6d8805bd09e9 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct682d6d8805bd09e9>() == 152, "AnonStruct682d6d8805bd09e9 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct682d6d8805bd09e9>() == 4, "AnonStruct682d6d8805bd09e9 alignment mismatch");

/// Element type for __AnonStruct_862498435f691a50
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct862498435f691a50 {
    pub exposed_machine: AnonStructE5cb3809f97dc82d,
    pub handle: RocStr,
    pub name: RocStr,
    pub semantic_id: RocStr,
    pub source: AnonStructDfa68fb5623390e9,
}

/// Element type for __AnonStruct_862498435f691a50
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct862498435f691a50 {
    pub exposed_machine: AnonStructE5cb3809f97dc82d,
    pub handle: RocStr,
    pub name: RocStr,
    pub semantic_id: RocStr,
    pub source: AnonStructDfa68fb5623390e9,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct862498435f691a50>() == 160, "AnonStruct862498435f691a50 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct862498435f691a50>() == 8, "AnonStruct862498435f691a50 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct862498435f691a50>() == 88, "AnonStruct862498435f691a50 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct862498435f691a50>() == 4, "AnonStruct862498435f691a50 alignment mismatch");

/// Element type for __AnonStruct_e5cb3809f97dc82d
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructE5cb3809f97dc82d {
    pub label: RocStr,
    pub semantic_id: RocStr,
}

/// Element type for __AnonStruct_e5cb3809f97dc82d
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructE5cb3809f97dc82d {
    pub label: RocStr,
    pub semantic_id: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructE5cb3809f97dc82d>() == 48, "AnonStructE5cb3809f97dc82d size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructE5cb3809f97dc82d>() == 8, "AnonStructE5cb3809f97dc82d alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructE5cb3809f97dc82d>() == 24, "AnonStructE5cb3809f97dc82d size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructE5cb3809f97dc82d>() == 4, "AnonStructE5cb3809f97dc82d alignment mismatch");

/// Element type for __AnonStruct_dfa68fb5623390e9
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructDfa68fb5623390e9 {
    pub uri: RocStr,
    pub range: AnonStructF854c29f636ef8f8,
}

/// Element type for __AnonStruct_dfa68fb5623390e9
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructDfa68fb5623390e9 {
    pub uri: RocStr,
    pub range: AnonStructF854c29f636ef8f8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructDfa68fb5623390e9>() == 40, "AnonStructDfa68fb5623390e9 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructDfa68fb5623390e9>() == 8, "AnonStructDfa68fb5623390e9 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructDfa68fb5623390e9>() == 28, "AnonStructDfa68fb5623390e9 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructDfa68fb5623390e9>() == 4, "AnonStructDfa68fb5623390e9 alignment mismatch");

/// Element type for __AnonStruct_5de797c596812777
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct5de797c596812777 {
    pub completeness: AnonStruct4433c02c1fd6f4,
    pub machine: AnonStructDc524cbc8f8e5592,
    pub model_digest: RocStr,
    pub nodes: RocList<AnonStructAb367e0f3bdf5730>,
    pub transitions: RocList<AnonStructFfd4ba00196aaf2>,
    pub view: AnonStruct862498435f691a50,
    pub schema_version: u32,
}

/// Element type for __AnonStruct_5de797c596812777
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct5de797c596812777 {
    pub completeness: AnonStruct4433c02c1fd6f4,
    pub machine: AnonStructDc524cbc8f8e5592,
    pub model_digest: RocStr,
    pub nodes: RocList<AnonStructAb367e0f3bdf5730>,
    pub transitions: RocList<AnonStructFfd4ba00196aaf2>,
    pub view: AnonStruct862498435f691a50,
    pub schema_version: u32,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct5de797c596812777>() == 360, "AnonStruct5de797c596812777 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct5de797c596812777>() == 8, "AnonStruct5de797c596812777 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct5de797c596812777>() == 196, "AnonStruct5de797c596812777 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct5de797c596812777>() == 4, "AnonStruct5de797c596812777 alignment mismatch");

/// Element type for __AnonStruct_4433c02c1fd6f4
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct4433c02c1fd6f4 {
    pub reasons: RocList<AnonStruct8ff265a5a21edc24>,
    pub complete: bool,
}

/// Element type for __AnonStruct_4433c02c1fd6f4
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct4433c02c1fd6f4 {
    pub reasons: RocList<AnonStruct8ff265a5a21edc24>,
    pub complete: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct4433c02c1fd6f4>() == 32, "AnonStruct4433c02c1fd6f4 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct4433c02c1fd6f4>() == 8, "AnonStruct4433c02c1fd6f4 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct4433c02c1fd6f4>() == 16, "AnonStruct4433c02c1fd6f4 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct4433c02c1fd6f4>() == 4, "AnonStruct4433c02c1fd6f4 alignment mismatch");

/// Element type for __AnonStruct_8ff265a5a21edc24
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct8ff265a5a21edc24 {
    pub code: RocStr,
    pub message: RocStr,
}

/// Element type for __AnonStruct_8ff265a5a21edc24
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct8ff265a5a21edc24 {
    pub code: RocStr,
    pub message: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct8ff265a5a21edc24>() == 48, "AnonStruct8ff265a5a21edc24 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct8ff265a5a21edc24>() == 8, "AnonStruct8ff265a5a21edc24 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct8ff265a5a21edc24>() == 24, "AnonStruct8ff265a5a21edc24 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct8ff265a5a21edc24>() == 4, "AnonStruct8ff265a5a21edc24 alignment mismatch");

/// Element type for __AnonStruct_dc524cbc8f8e5592
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructDc524cbc8f8e5592 {
    pub label: RocStr,
    pub semantic_id: RocStr,
    pub source: AnonStructDfa68fb5623390e9,
}

/// Element type for __AnonStruct_dc524cbc8f8e5592
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructDc524cbc8f8e5592 {
    pub label: RocStr,
    pub semantic_id: RocStr,
    pub source: AnonStructDfa68fb5623390e9,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructDc524cbc8f8e5592>() == 88, "AnonStructDc524cbc8f8e5592 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructDc524cbc8f8e5592>() == 8, "AnonStructDc524cbc8f8e5592 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructDc524cbc8f8e5592>() == 52, "AnonStructDc524cbc8f8e5592 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructDc524cbc8f8e5592>() == 4, "AnonStructDc524cbc8f8e5592 alignment mismatch");

/// Element type for __AnonStruct_ab367e0f3bdf5730
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructAb367e0f3bdf5730 {
    pub label: RocStr,
    pub semantic_id: RocStr,
    pub source: AnonStructDfa68fb5623390e9,
    pub kind: u8,
}

/// Element type for __AnonStruct_ab367e0f3bdf5730
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructAb367e0f3bdf5730 {
    pub label: RocStr,
    pub semantic_id: RocStr,
    pub source: AnonStructDfa68fb5623390e9,
    pub kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructAb367e0f3bdf5730>() == 96, "AnonStructAb367e0f3bdf5730 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructAb367e0f3bdf5730>() == 8, "AnonStructAb367e0f3bdf5730 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructAb367e0f3bdf5730>() == 56, "AnonStructAb367e0f3bdf5730 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructAb367e0f3bdf5730>() == 4, "AnonStructAb367e0f3bdf5730 alignment mismatch");

/// Element type for __AnonStruct_ffd4ba00196aaf2
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructFfd4ba00196aaf2 {
    pub effect: AnonStruct5bcf2c5d20eeb133,
    pub guard: AnonStruct5bcf2c5d20eeb133,
    pub label: NoneOrSomeType63,
    pub semantic_id: RocStr,
    pub source: RocStr,
    pub source_reference: AnonStructDfa68fb5623390e9,
    pub target: RocStr,
    pub trigger: AnonStruct7d92a9ce2339c3f2,
    pub implied: bool,
}

/// Element type for __AnonStruct_ffd4ba00196aaf2
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStructFfd4ba00196aaf2 {
    pub effect: AnonStruct5bcf2c5d20eeb133,
    pub guard: AnonStruct5bcf2c5d20eeb133,
    pub label: NoneOrSomeType63,
    pub semantic_id: RocStr,
    pub source: RocStr,
    pub source_reference: AnonStructDfa68fb5623390e9,
    pub target: RocStr,
    pub trigger: AnonStruct7d92a9ce2339c3f2,
    pub implied: bool,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStructFfd4ba00196aaf2>() == 640, "AnonStructFfd4ba00196aaf2 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStructFfd4ba00196aaf2>() == 8, "AnonStructFfd4ba00196aaf2 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStructFfd4ba00196aaf2>() == 352, "AnonStructFfd4ba00196aaf2 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStructFfd4ba00196aaf2>() == 4, "AnonStructFfd4ba00196aaf2 alignment mismatch");

/// Element type for __AnonStruct_5bcf2c5d20eeb133
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct5bcf2c5d20eeb133 {
    pub label: NoneOrSomeType63,
    pub source: NoneOrSomeType64,
    pub unsupported: NoneOrSomeType65,
    pub kind: u8,
}

/// Element type for __AnonStruct_5bcf2c5d20eeb133
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct5bcf2c5d20eeb133 {
    pub label: NoneOrSomeType63,
    pub source: NoneOrSomeType64,
    pub unsupported: NoneOrSomeType65,
    pub kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct5bcf2c5d20eeb133>() == 144, "AnonStruct5bcf2c5d20eeb133 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct5bcf2c5d20eeb133>() == 8, "AnonStruct5bcf2c5d20eeb133 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct5bcf2c5d20eeb133>() == 80, "AnonStruct5bcf2c5d20eeb133 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct5bcf2c5d20eeb133>() == 4, "AnonStruct5bcf2c5d20eeb133 alignment mismatch");

/// Element type for __AnonStruct_7d92a9ce2339c3f2
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct7d92a9ce2339c3f2 {
    pub label: NoneOrSomeType63,
    pub source: NoneOrSomeType64,
    pub target: NoneOrSomeType67,
    pub unsupported: NoneOrSomeType65,
    pub kind: u8,
}

/// Element type for __AnonStruct_7d92a9ce2339c3f2
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct7d92a9ce2339c3f2 {
    pub label: NoneOrSomeType63,
    pub source: NoneOrSomeType64,
    pub target: NoneOrSomeType67,
    pub unsupported: NoneOrSomeType65,
    pub kind: u8,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct7d92a9ce2339c3f2>() == 200, "AnonStruct7d92a9ce2339c3f2 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct7d92a9ce2339c3f2>() == 8, "AnonStruct7d92a9ce2339c3f2 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct7d92a9ce2339c3f2>() == 108, "AnonStruct7d92a9ce2339c3f2 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct7d92a9ce2339c3f2>() == 4, "AnonStruct7d92a9ce2339c3f2 alignment mismatch");

/// Element type for __AnonStruct_3f5ac9322a5c230f
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct3f5ac9322a5c230f {
    pub contents: RocListWith<u8, false>,
    pub file_path: RocStr,
}

/// Element type for __AnonStruct_3f5ac9322a5c230f
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct AnonStruct3f5ac9322a5c230f {
    pub contents: RocListWith<u8, false>,
    pub file_path: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<AnonStruct3f5ac9322a5c230f>() == 48, "AnonStruct3f5ac9322a5c230f size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<AnonStruct3f5ac9322a5c230f>() == 8, "AnonStruct3f5ac9322a5c230f alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<AnonStruct3f5ac9322a5c230f>() == 24, "AnonStruct3f5ac9322a5c230f size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<AnonStruct3f5ac9322a5c230f>() == 4, "AnonStruct3f5ac9322a5c230f alignment mismatch");

/// Tag union: DebugOrErrorOrInfoOrWarning
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum DebugOrErrorOrInfoOrWarning {
    Debug = 0,
    Error = 1,
    Info = 2,
    Warning = 3,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<DebugOrErrorOrInfoOrWarning>() == 1, "DebugOrErrorOrInfoOrWarning size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<DebugOrErrorOrInfoOrWarning>() == 1, "DebugOrErrorOrInfoOrWarning alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<DebugOrErrorOrInfoOrWarning>() == 1, "DebugOrErrorOrInfoOrWarning size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<DebugOrErrorOrInfoOrWarning>() == 1, "DebugOrErrorOrInfoOrWarning alignment mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType3Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType3Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<RocStr>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType3PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType3 {
    pub _payload_alignment: [NoneOrSomeType3PayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: NoneOrSomeType3Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType3 {
    pub payload: NoneOrSomeType3Payload,
    pub tag: NoneOrSomeType3Tag,
}

impl NoneOrSomeType3 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType3Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType3Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType3Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType3Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType3>() == 32, "NoneOrSomeType3 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType3>() == 8, "NoneOrSomeType3 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType3, tag) == 24, "NoneOrSomeType3 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType3>() == 16, "NoneOrSomeType3 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType3>() == 4, "NoneOrSomeType3 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType3, tag) == 12, "NoneOrSomeType3 tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostModelChildrenResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostModelChildrenResultPayload {
    pub err: core::mem::ManuallyDrop<RocStr>,
    pub ok: core::mem::ManuallyDrop<RocList<AnonStructD19c6c897d0b2f23>>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostModelChildrenResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelChildrenResult {
    pub _payload_alignment: [HostModelChildrenResultPayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: HostModelChildrenResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelChildrenResult {
    pub payload: HostModelChildrenResultPayload,
    pub tag: HostModelChildrenResultTag,
}

impl HostModelChildrenResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelChildrenResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelChildrenResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelChildrenResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelChildrenResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelChildrenResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStructD19c6c897d0b2f23> {
        unsafe { &*(self.payload.as_ptr() as *const RocList<AnonStructD19c6c897d0b2f23>) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelChildrenResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStructD19c6c897d0b2f23> {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<RocList<AnonStructD19c6c897d0b2f23>> as *const RocList<AnonStructD19c6c897d0b2f23>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelChildrenResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStructD19c6c897d0b2f23> {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocList<AnonStructD19c6c897d0b2f23>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelChildrenResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStructD19c6c897d0b2f23> {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostModelChildrenResult>() == 32, "HostModelChildrenResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostModelChildrenResult>() == 8, "HostModelChildrenResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostModelChildrenResult, tag) == 24, "HostModelChildrenResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostModelChildrenResult>() == 16, "HostModelChildrenResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostModelChildrenResult>() == 4, "HostModelChildrenResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostModelChildrenResult, tag) == 12, "HostModelChildrenResult tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType11Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType11Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<RocStr>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType11PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType11 {
    pub _payload_alignment: [NoneOrSomeType11PayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: NoneOrSomeType11Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType11 {
    pub payload: NoneOrSomeType11Payload,
    pub tag: NoneOrSomeType11Tag,
}

impl NoneOrSomeType11 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType11Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType11Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType11Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType11Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType11>() == 32, "NoneOrSomeType11 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType11>() == 8, "NoneOrSomeType11 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType11, tag) == 24, "NoneOrSomeType11 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType11>() == 16, "NoneOrSomeType11 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType11>() == 4, "NoneOrSomeType11 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType11, tag) == 12, "NoneOrSomeType11 tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType12Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType12Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<RocStr>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType12PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType12 {
    pub _payload_alignment: [NoneOrSomeType12PayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: NoneOrSomeType12Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType12 {
    pub payload: NoneOrSomeType12Payload,
    pub tag: NoneOrSomeType12Tag,
}

impl NoneOrSomeType12 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType12Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType12Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType12Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType12Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType12>() == 32, "NoneOrSomeType12 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType12>() == 8, "NoneOrSomeType12 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType12, tag) == 24, "NoneOrSomeType12 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType12>() == 16, "NoneOrSomeType12 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType12>() == 4, "NoneOrSomeType12 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType12, tag) == 12, "NoneOrSomeType12 tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostModelElementResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostModelElementResultPayload {
    pub err: core::mem::ManuallyDrop<RocStr>,
    pub ok: core::mem::ManuallyDrop<AnonStruct1ebccf7a1ff71691>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostModelElementResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelElementResult {
    pub _payload_alignment: [HostModelElementResultPayloadAlignment; 0],
    pub payload: [u8; 320],
    pub tag: HostModelElementResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelElementResult {
    pub payload: HostModelElementResultPayload,
    pub tag: HostModelElementResultTag,
}

impl HostModelElementResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelElementResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelElementResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelElementResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelElementResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelElementResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct1ebccf7a1ff71691 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct1ebccf7a1ff71691) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelElementResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct1ebccf7a1ff71691 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<AnonStruct1ebccf7a1ff71691> as *const AnonStruct1ebccf7a1ff71691) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelElementResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct1ebccf7a1ff71691 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct1ebccf7a1ff71691) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelElementResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct1ebccf7a1ff71691 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostModelElementResult>() == 616, "HostModelElementResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostModelElementResult>() == 8, "HostModelElementResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostModelElementResult, tag) == 608, "HostModelElementResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostModelElementResult>() == 324, "HostModelElementResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostModelElementResult>() == 4, "HostModelElementResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostModelElementResult, tag) == 320, "HostModelElementResult tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType17Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType17Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<bool>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(1))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType17PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType17 {
    pub _payload_alignment: [NoneOrSomeType17PayloadAlignment; 0],
    pub payload: [u8; 1],
    pub tag: NoneOrSomeType17Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType17 {
    pub payload: NoneOrSomeType17Payload,
    pub tag: NoneOrSomeType17Tag,
}

impl NoneOrSomeType17 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType17Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &bool {
        unsafe { &*(self.payload.as_ptr() as *const bool) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType17Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &bool {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<bool> as *const bool) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType17Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> bool {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const bool) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType17Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> bool {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType17>() == 2, "NoneOrSomeType17 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType17>() == 1, "NoneOrSomeType17 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType17, tag) == 1, "NoneOrSomeType17 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType17>() == 2, "NoneOrSomeType17 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType17>() == 1, "NoneOrSomeType17 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType17, tag) == 1, "NoneOrSomeType17 tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType18Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType18Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<RocStr>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType18PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType18 {
    pub _payload_alignment: [NoneOrSomeType18PayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: NoneOrSomeType18Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType18 {
    pub payload: NoneOrSomeType18Payload,
    pub tag: NoneOrSomeType18Tag,
}

impl NoneOrSomeType18 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType18Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType18Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType18Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType18Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType18>() == 32, "NoneOrSomeType18 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType18>() == 8, "NoneOrSomeType18 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType18, tag) == 24, "NoneOrSomeType18 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType18>() == 16, "NoneOrSomeType18 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType18>() == 4, "NoneOrSomeType18 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType18, tag) == 12, "NoneOrSomeType18 tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType19Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType19Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<AnonStructE7b668836e719b96>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType19PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType19 {
    pub _payload_alignment: [NoneOrSomeType19PayloadAlignment; 0],
    pub payload: [u8; 36],
    pub tag: NoneOrSomeType19Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType19 {
    pub payload: NoneOrSomeType19Payload,
    pub tag: NoneOrSomeType19Tag,
}

impl NoneOrSomeType19 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType19Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStructE7b668836e719b96 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStructE7b668836e719b96) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType19Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStructE7b668836e719b96 {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<AnonStructE7b668836e719b96> as *const AnonStructE7b668836e719b96) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType19Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStructE7b668836e719b96 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStructE7b668836e719b96) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType19Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStructE7b668836e719b96 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType19>() == 80, "NoneOrSomeType19 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType19>() == 8, "NoneOrSomeType19 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType19, tag) == 72, "NoneOrSomeType19 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType19>() == 40, "NoneOrSomeType19 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType19>() == 4, "NoneOrSomeType19 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType19, tag) == 36, "NoneOrSomeType19 tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType21Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType21Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<AnonStructD19c6c897d0b2f23>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType21PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType21 {
    pub _payload_alignment: [NoneOrSomeType21PayloadAlignment; 0],
    pub payload: [u8; 68],
    pub tag: NoneOrSomeType21Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType21 {
    pub payload: NoneOrSomeType21Payload,
    pub tag: NoneOrSomeType21Tag,
}

impl NoneOrSomeType21 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType21Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStructD19c6c897d0b2f23 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStructD19c6c897d0b2f23) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType21Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStructD19c6c897d0b2f23 {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<AnonStructD19c6c897d0b2f23> as *const AnonStructD19c6c897d0b2f23) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType21Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStructD19c6c897d0b2f23 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStructD19c6c897d0b2f23) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType21Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStructD19c6c897d0b2f23 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType21>() == 144, "NoneOrSomeType21 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType21>() == 8, "NoneOrSomeType21 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType21, tag) == 136, "NoneOrSomeType21 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType21>() == 72, "NoneOrSomeType21 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType21>() == 4, "NoneOrSomeType21 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType21, tag) == 68, "NoneOrSomeType21 tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostModelTypedByResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostModelTypedByResultPayload {
    pub err: core::mem::ManuallyDrop<RocStr>,
    pub ok: core::mem::ManuallyDrop<NoneOrSomeType27>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostModelTypedByResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelTypedByResult {
    pub _payload_alignment: [HostModelTypedByResultPayloadAlignment; 0],
    pub payload: [u8; 72],
    pub tag: HostModelTypedByResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelTypedByResult {
    pub payload: HostModelTypedByResultPayload,
    pub tag: HostModelTypedByResultTag,
}

impl HostModelTypedByResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelTypedByResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelTypedByResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelTypedByResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelTypedByResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelTypedByResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &NoneOrSomeType27 {
        unsafe { &*(self.payload.as_ptr() as *const NoneOrSomeType27) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelTypedByResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &NoneOrSomeType27 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<NoneOrSomeType27> as *const NoneOrSomeType27) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelTypedByResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> NoneOrSomeType27 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const NoneOrSomeType27) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelTypedByResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> NoneOrSomeType27 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostModelTypedByResult>() == 152, "HostModelTypedByResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostModelTypedByResult>() == 8, "HostModelTypedByResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostModelTypedByResult, tag) == 144, "HostModelTypedByResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostModelTypedByResult>() == 76, "HostModelTypedByResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostModelTypedByResult>() == 4, "HostModelTypedByResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostModelTypedByResult, tag) == 72, "HostModelTypedByResult tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType27Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType27Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<AnonStructD19c6c897d0b2f23>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType27PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType27 {
    pub _payload_alignment: [NoneOrSomeType27PayloadAlignment; 0],
    pub payload: [u8; 68],
    pub tag: NoneOrSomeType27Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType27 {
    pub payload: NoneOrSomeType27Payload,
    pub tag: NoneOrSomeType27Tag,
}

impl NoneOrSomeType27 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType27Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStructD19c6c897d0b2f23 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStructD19c6c897d0b2f23) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType27Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStructD19c6c897d0b2f23 {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<AnonStructD19c6c897d0b2f23> as *const AnonStructD19c6c897d0b2f23) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType27Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStructD19c6c897d0b2f23 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStructD19c6c897d0b2f23) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType27Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStructD19c6c897d0b2f23 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType27>() == 144, "NoneOrSomeType27 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType27>() == 8, "NoneOrSomeType27 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType27, tag) == 136, "NoneOrSomeType27 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType27>() == 72, "NoneOrSomeType27 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType27>() == 4, "NoneOrSomeType27 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType27, tag) == 68, "NoneOrSomeType27 tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType30Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType30Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<RocStr>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType30PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType30 {
    pub _payload_alignment: [NoneOrSomeType30PayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: NoneOrSomeType30Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType30 {
    pub payload: NoneOrSomeType30Payload,
    pub tag: NoneOrSomeType30Tag,
}

impl NoneOrSomeType30 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType30Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType30Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType30Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType30Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType30>() == 32, "NoneOrSomeType30 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType30>() == 8, "NoneOrSomeType30 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType30, tag) == 24, "NoneOrSomeType30 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType30>() == 16, "NoneOrSomeType30 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType30>() == 4, "NoneOrSomeType30 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType30, tag) == 12, "NoneOrSomeType30 tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostModelRelationshipsResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostModelRelationshipsResultPayload {
    pub err: core::mem::ManuallyDrop<RocStr>,
    pub ok: core::mem::ManuallyDrop<RocList<AnonStruct682d6d8805bd09e9>>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostModelRelationshipsResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelRelationshipsResult {
    pub _payload_alignment: [HostModelRelationshipsResultPayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: HostModelRelationshipsResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelRelationshipsResult {
    pub payload: HostModelRelationshipsResultPayload,
    pub tag: HostModelRelationshipsResultTag,
}

impl HostModelRelationshipsResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelRelationshipsResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelRelationshipsResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelRelationshipsResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelRelationshipsResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelRelationshipsResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStruct682d6d8805bd09e9> {
        unsafe { &*(self.payload.as_ptr() as *const RocList<AnonStruct682d6d8805bd09e9>) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelRelationshipsResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStruct682d6d8805bd09e9> {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<RocList<AnonStruct682d6d8805bd09e9>> as *const RocList<AnonStruct682d6d8805bd09e9>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelRelationshipsResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStruct682d6d8805bd09e9> {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocList<AnonStruct682d6d8805bd09e9>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostModelRelationshipsResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStruct682d6d8805bd09e9> {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostModelRelationshipsResult>() == 32, "HostModelRelationshipsResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostModelRelationshipsResult>() == 8, "HostModelRelationshipsResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostModelRelationshipsResult, tag) == 24, "HostModelRelationshipsResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostModelRelationshipsResult>() == 16, "HostModelRelationshipsResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostModelRelationshipsResult>() == 4, "HostModelRelationshipsResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostModelRelationshipsResult, tag) == 12, "HostModelRelationshipsResult tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType37Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType37Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<RocStr>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType37PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType37 {
    pub _payload_alignment: [NoneOrSomeType37PayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: NoneOrSomeType37Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType37 {
    pub payload: NoneOrSomeType37Payload,
    pub tag: NoneOrSomeType37Tag,
}

impl NoneOrSomeType37 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType37Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType37Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType37Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType37Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType37>() == 32, "NoneOrSomeType37 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType37>() == 8, "NoneOrSomeType37 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType37, tag) == 24, "NoneOrSomeType37 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType37>() == 16, "NoneOrSomeType37 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType37>() == 4, "NoneOrSomeType37 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType37, tag) == 12, "NoneOrSomeType37 tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostStateTransitionViewsResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostStateTransitionViewsResultPayload {
    pub err: core::mem::ManuallyDrop<RocStr>,
    pub ok: core::mem::ManuallyDrop<RocList<AnonStruct862498435f691a50>>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostStateTransitionViewsResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostStateTransitionViewsResult {
    pub _payload_alignment: [HostStateTransitionViewsResultPayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: HostStateTransitionViewsResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostStateTransitionViewsResult {
    pub payload: HostStateTransitionViewsResultPayload,
    pub tag: HostStateTransitionViewsResultTag,
}

impl HostStateTransitionViewsResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewsResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewsResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewsResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewsResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewsResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStruct862498435f691a50> {
        unsafe { &*(self.payload.as_ptr() as *const RocList<AnonStruct862498435f691a50>) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewsResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStruct862498435f691a50> {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<RocList<AnonStruct862498435f691a50>> as *const RocList<AnonStruct862498435f691a50>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewsResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStruct862498435f691a50> {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocList<AnonStruct862498435f691a50>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewsResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStruct862498435f691a50> {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostStateTransitionViewsResult>() == 32, "HostStateTransitionViewsResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostStateTransitionViewsResult>() == 8, "HostStateTransitionViewsResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostStateTransitionViewsResult, tag) == 24, "HostStateTransitionViewsResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostStateTransitionViewsResult>() == 16, "HostStateTransitionViewsResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostStateTransitionViewsResult>() == 4, "HostStateTransitionViewsResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostStateTransitionViewsResult, tag) == 12, "HostStateTransitionViewsResult tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum HostStateTransitionViewResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union HostStateTransitionViewResultPayload {
    pub err: core::mem::ManuallyDrop<RocStr>,
    pub ok: core::mem::ManuallyDrop<AnonStruct5de797c596812777>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct HostStateTransitionViewResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostStateTransitionViewResult {
    pub _payload_alignment: [HostStateTransitionViewResultPayloadAlignment; 0],
    pub payload: [u8; 196],
    pub tag: HostStateTransitionViewResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostStateTransitionViewResult {
    pub payload: HostStateTransitionViewResultPayload,
    pub tag: HostStateTransitionViewResultTag,
}

impl HostStateTransitionViewResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct5de797c596812777 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct5de797c596812777) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &AnonStruct5de797c596812777 {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<AnonStruct5de797c596812777> as *const AnonStruct5de797c596812777) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct5de797c596812777 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct5de797c596812777) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `HostStateTransitionViewResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> AnonStruct5de797c596812777 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostStateTransitionViewResult>() == 368, "HostStateTransitionViewResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostStateTransitionViewResult>() == 8, "HostStateTransitionViewResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(HostStateTransitionViewResult, tag) == 360, "HostStateTransitionViewResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostStateTransitionViewResult>() == 200, "HostStateTransitionViewResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostStateTransitionViewResult>() == 4, "HostStateTransitionViewResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(HostStateTransitionViewResult, tag) == 196, "HostStateTransitionViewResult tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType63Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType63Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<RocStr>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType63PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType63 {
    pub _payload_alignment: [NoneOrSomeType63PayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: NoneOrSomeType63Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType63 {
    pub payload: NoneOrSomeType63Payload,
    pub tag: NoneOrSomeType63Tag,
}

impl NoneOrSomeType63 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType63Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType63Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType63Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType63Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType63>() == 32, "NoneOrSomeType63 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType63>() == 8, "NoneOrSomeType63 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType63, tag) == 24, "NoneOrSomeType63 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType63>() == 16, "NoneOrSomeType63 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType63>() == 4, "NoneOrSomeType63 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType63, tag) == 12, "NoneOrSomeType63 tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType64Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType64Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<AnonStructDfa68fb5623390e9>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType64PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType64 {
    pub _payload_alignment: [NoneOrSomeType64PayloadAlignment; 0],
    pub payload: [u8; 28],
    pub tag: NoneOrSomeType64Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType64 {
    pub payload: NoneOrSomeType64Payload,
    pub tag: NoneOrSomeType64Tag,
}

impl NoneOrSomeType64 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType64Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStructDfa68fb5623390e9 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStructDfa68fb5623390e9) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType64Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStructDfa68fb5623390e9 {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<AnonStructDfa68fb5623390e9> as *const AnonStructDfa68fb5623390e9) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType64Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStructDfa68fb5623390e9 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStructDfa68fb5623390e9) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType64Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStructDfa68fb5623390e9 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType64>() == 48, "NoneOrSomeType64 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType64>() == 8, "NoneOrSomeType64 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType64, tag) == 40, "NoneOrSomeType64 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType64>() == 32, "NoneOrSomeType64 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType64>() == 4, "NoneOrSomeType64 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType64, tag) == 28, "NoneOrSomeType64 tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType65Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType65Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<AnonStruct8ff265a5a21edc24>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType65PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType65 {
    pub _payload_alignment: [NoneOrSomeType65PayloadAlignment; 0],
    pub payload: [u8; 24],
    pub tag: NoneOrSomeType65Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType65 {
    pub payload: NoneOrSomeType65Payload,
    pub tag: NoneOrSomeType65Tag,
}

impl NoneOrSomeType65 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType65Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStruct8ff265a5a21edc24 {
        unsafe { &*(self.payload.as_ptr() as *const AnonStruct8ff265a5a21edc24) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType65Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStruct8ff265a5a21edc24 {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<AnonStruct8ff265a5a21edc24> as *const AnonStruct8ff265a5a21edc24) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType65Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStruct8ff265a5a21edc24 {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStruct8ff265a5a21edc24) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType65Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStruct8ff265a5a21edc24 {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType65>() == 56, "NoneOrSomeType65 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType65>() == 8, "NoneOrSomeType65 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType65, tag) == 48, "NoneOrSomeType65 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType65>() == 28, "NoneOrSomeType65 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType65>() == 4, "NoneOrSomeType65 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType65, tag) == 24, "NoneOrSomeType65 tag offset mismatch");

/// Tag discriminant for NoneOrSome.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum NoneOrSomeType67Tag {
    None = 0,
    Some = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union NoneOrSomeType67Payload {
    pub none: [u8; 0],
    pub some: core::mem::ManuallyDrop<AnonStructE5cb3809f97dc82d>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType67PayloadAlignment;

/// Tag union: NoneOrSome
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType67 {
    pub _payload_alignment: [NoneOrSomeType67PayloadAlignment; 0],
    pub payload: [u8; 24],
    pub tag: NoneOrSomeType67Tag,
}

/// Tag union: NoneOrSome
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct NoneOrSomeType67 {
    pub payload: NoneOrSomeType67Payload,
    pub tag: NoneOrSomeType67Tag,
}

impl NoneOrSomeType67 {
    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType67Tag::Some` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStructE5cb3809f97dc82d {
        unsafe { &*(self.payload.as_ptr() as *const AnonStructE5cb3809f97dc82d) }
    }

    /// Borrow the `Some` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType67Tag::Some` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_some_unchecked(&self) -> &AnonStructE5cb3809f97dc82d {
        unsafe { &*(&self.payload.some as *const core::mem::ManuallyDrop<AnonStructE5cb3809f97dc82d> as *const AnonStructE5cb3809f97dc82d) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType67Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStructE5cb3809f97dc82d {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const AnonStructE5cb3809f97dc82d) }
    }

    /// Move the `Some` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `NoneOrSomeType67Tag::Some`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_some_unchecked(&mut self) -> AnonStructE5cb3809f97dc82d {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.some) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType67>() == 56, "NoneOrSomeType67 size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType67>() == 8, "NoneOrSomeType67 alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType67, tag) == 48, "NoneOrSomeType67 tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<NoneOrSomeType67>() == 28, "NoneOrSomeType67 size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<NoneOrSomeType67>() == 4, "NoneOrSomeType67 alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(NoneOrSomeType67, tag) == 24, "NoneOrSomeType67 tag offset mismatch");

/// Tag discriminant for Try.
#[repr(u8)]
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum GenerateForHostResultTag {
    Err = 0,
    Ok = 1,
}

#[repr(C)]
#[derive(Clone, Copy)]
pub union GenerateForHostResultPayload {
    pub err: core::mem::ManuallyDrop<RocStr>,
    pub ok: core::mem::ManuallyDrop<RocList<AnonStruct3f5ac9322a5c230f>>,
}

#[cfg(target_pointer_width = "32")]
#[repr(align(4))]
#[derive(Clone, Copy)]
pub struct GenerateForHostResultPayloadAlignment;

/// Tag union: Try
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct GenerateForHostResult {
    pub _payload_alignment: [GenerateForHostResultPayloadAlignment; 0],
    pub payload: [u8; 12],
    pub tag: GenerateForHostResultTag,
}

/// Tag union: Try
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct GenerateForHostResult {
    pub payload: GenerateForHostResultPayload,
    pub tag: GenerateForHostResultTag,
}

impl GenerateForHostResult {
    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `GenerateForHostResultTag::Err` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(self.payload.as_ptr() as *const RocStr) }
    }

    /// Borrow the `Err` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `GenerateForHostResultTag::Err` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_err_unchecked(&self) -> &RocStr {
        unsafe { &*(&self.payload.err as *const core::mem::ManuallyDrop<RocStr> as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `GenerateForHostResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocStr) }
    }

    /// Move the `Err` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `GenerateForHostResultTag::Err`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_err_unchecked(&mut self) -> RocStr {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.err) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `GenerateForHostResultTag::Ok` and the payload must still be initialized.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStruct3f5ac9322a5c230f> {
        unsafe { &*(self.payload.as_ptr() as *const RocList<AnonStruct3f5ac9322a5c230f>) }
    }

    /// Borrow the `Ok` payload without creating another owner.
    ///
    /// # Safety
    /// `self.tag` must be `GenerateForHostResultTag::Ok` and the payload must still be initialized.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn borrow_payload_ok_unchecked(&self) -> &RocList<AnonStruct3f5ac9322a5c230f> {
        unsafe { &*(&self.payload.ok as *const core::mem::ManuallyDrop<RocList<AnonStruct3f5ac9322a5c230f>> as *const RocList<AnonStruct3f5ac9322a5c230f>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `GenerateForHostResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(target_pointer_width = "32")]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStruct3f5ac9322a5c230f> {
        unsafe { core::ptr::read(self.payload.as_ptr() as *const RocList<AnonStruct3f5ac9322a5c230f>) }
    }

    /// Move the `Ok` payload out of one owned tag-union shell.
    ///
    /// # Safety
    /// `self.tag` must be `GenerateForHostResultTag::Ok`. After this call, `self` is logically uninitialized and must not be read or destroyed.
    #[cfg(not(target_pointer_width = "32"))]
    pub unsafe fn take_payload_ok_unchecked(&mut self) -> RocList<AnonStruct3f5ac9322a5c230f> {
        unsafe { core::mem::ManuallyDrop::take(&mut self.payload.ok) }
    }

}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<GenerateForHostResult>() == 32, "GenerateForHostResult size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<GenerateForHostResult>() == 8, "GenerateForHostResult alignment mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::offset_of!(GenerateForHostResult, tag) == 24, "GenerateForHostResult tag offset mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<GenerateForHostResult>() == 16, "GenerateForHostResult size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<GenerateForHostResult>() == 4, "GenerateForHostResult alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::offset_of!(GenerateForHostResult, tag) == 12, "GenerateForHostResult tag offset mismatch");

/// Return type record for HostModel.info!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(target_pointer_width = "32")]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelInfoRetRecord {
    pub model_digest: RocStr,
    pub semantic_api_version: RocStr,
    pub spec42_version: RocStr,
}

/// Return type record for HostModel.info!
/// Fields ordered by compiler-emitted ABI offsets.
#[cfg(not(target_pointer_width = "32"))]
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelInfoRetRecord {
    pub model_digest: RocStr,
    pub semantic_api_version: RocStr,
    pub spec42_version: RocStr,
}

#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::size_of::<HostModelInfoRetRecord>() == 72, "HostModelInfoRetRecord size mismatch");
#[cfg(target_pointer_width = "64")]
const _: () = assert!(core::mem::align_of::<HostModelInfoRetRecord>() == 8, "HostModelInfoRetRecord alignment mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::size_of::<HostModelInfoRetRecord>() == 36, "HostModelInfoRetRecord size mismatch");
#[cfg(target_pointer_width = "32")]
const _: () = assert!(core::mem::align_of::<HostModelInfoRetRecord>() == 4, "HostModelInfoRetRecord alignment mismatch");

/// Arguments for HostDiagnostics.log!
/// Roc signature: [Debug, Error, Info, Warning], Str => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostDiagnosticsLogArgs {
    pub arg0: DebugOrErrorOrInfoOrWarning,
    pub arg1: RocStr,
}

/// Arguments for HostDiagnostics.report!
/// Roc signature: [Debug, Error, Info, Warning], Str, [None, Some(Str)] => {}
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostDiagnosticsReportArgs {
    pub arg0: DebugOrErrorOrInfoOrWarning,
    pub arg1: RocStr,
    pub arg2: NoneOrSomeType3,
}

/// Arguments for HostModel.children!
/// Roc signature: Str => Try(List({ handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str }), Str)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelChildrenArgs {
    pub arg0: RocStr,
}

/// Arguments for HostModel.effective_features!
/// Roc signature: Str => Try(List({ handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str }), Str)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelEffectiveFeaturesArgs {
    pub arg0: RocStr,
}

/// Arguments for HostModel.element!
/// Roc signature: Str => Try({ abstract_flag : Bool, composite : [None, Some(Bool)], conjugated : Bool, constant : Bool, declared_name : [None, Some(Str)], definition : Bool, derived : Bool, direction : [None, Some(Str)], documentation : [None, Some(Str)], effective_name : [None, Some(Str)], end : Bool, evaluated_value : [None, Some(Str)], individual : Bool, multiplicity : [None, Some({ implied : Bool, lower : [None, Some(Str)], ordered : Bool, unique : [None, Some(Bool)], upper : [None, Some(Str)] })], ordered : [None, Some(Bool)], owner : [None, Some({ handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str })], reference : [None, Some(Bool)], short_name : [None, Some(Str)], source_range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, source_uri : Str, summary : { handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str }, unique : [None, Some(Bool)], variation : Bool }, Str)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelElementArgs {
    pub arg0: RocStr,
}

/// Arguments for HostModel.find!
/// Roc signature: [None, Some(Str)] => Try(List({ handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str }), Str)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelFindArgs {
    pub arg0: NoneOrSomeType12,
}

/// Arguments for HostModel.relationships!
/// Roc signature: Str => Try(List({ implied : Bool, kind : Str, source : { handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str }, target : { handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str } }), Str)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelRelationshipsArgs {
    pub arg0: RocStr,
}

/// Arguments for HostModel.typed_by!
/// Roc signature: Str => Try([None, Some({ handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str })], Str)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostModelTypedByArgs {
    pub arg0: RocStr,
}

/// Arguments for HostStateTransition.view!
/// Roc signature: Str => Try({ completeness : { complete : Bool, reasons : List({ code : Str, message : Str }) }, machine : { label : Str, semantic_id : Str, source : { range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str } }, model_digest : Str, nodes : List({ kind : U8, label : Str, semantic_id : Str, source : { range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str } }), schema_version : U32, transitions : List({ effect : { kind : U8, label : [None, Some(Str)], source : [None, Some({ range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str })], unsupported : [None, Some({ code : Str, message : Str })] }, guard : { kind : U8, label : [None, Some(Str)], source : [None, Some({ range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str })], unsupported : [None, Some({ code : Str, message : Str })] }, implied : Bool, label : [None, Some(Str)], semantic_id : Str, source : Str, source_reference : { range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str }, target : Str, trigger : { kind : U8, label : [None, Some(Str)], source : [None, Some({ range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str })], target : [None, Some({ label : Str, semantic_id : Str })], unsupported : [None, Some({ code : Str, message : Str })] } }), view : { exposed_machine : { label : Str, semantic_id : Str }, handle : Str, name : Str, semantic_id : Str, source : { range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str } } }, Str)
/// Refcounted fields are owned by the hosted function.
#[repr(C)]
#[derive(Clone, Copy)]
pub struct HostStateTransitionViewArgs {
    pub arg0: RocStr,
}

// Platform Type Aliases

pub type NoneOrSome = NoneOrSomeType3;
pub type NoneOrSomePayload = NoneOrSomeType3Payload;
pub type NoneOrSomeTag = NoneOrSomeType3Tag;
pub type HostModelChildrenOk = AnonStructD19c6c897d0b2f23;
pub type HostModelEffectiveFeaturesResult = HostModelChildrenResult;
pub type HostModelEffectiveFeaturesResultPayload = HostModelChildrenResultPayload;
pub type HostModelEffectiveFeaturesResultTag = HostModelChildrenResultTag;
pub type HostModelEffectiveFeaturesOk = AnonStructD19c6c897d0b2f23;
pub type HostModelElementOk = AnonStruct1ebccf7a1ff71691;
pub type NoneOrSomeSome = AnonStructE7b668836e719b96;
pub type HostModelElementOkSummary = AnonStructD19c6c897d0b2f23;
pub type HostModelElementOkSourceRange = AnonStructF854c29f636ef8f8;
pub type HostModelFindResult = HostModelChildrenResult;
pub type HostModelFindResultPayload = HostModelChildrenResultPayload;
pub type HostModelFindResultTag = HostModelChildrenResultTag;
pub type HostModelFindOk = AnonStructD19c6c897d0b2f23;
pub type HostModelInfo = AnonStruct5f154a81aee47322;
pub type HostModelRelationshipsOk = AnonStruct682d6d8805bd09e9;
pub type HostModelRelationshipsOkSource = AnonStructD19c6c897d0b2f23;
pub type HostModelRelationshipsOkTarget = AnonStructD19c6c897d0b2f23;
pub type HostModelRootsResult = HostModelChildrenResult;
pub type HostModelRootsResultPayload = HostModelChildrenResultPayload;
pub type HostModelRootsResultTag = HostModelChildrenResultTag;
pub type HostModelRootsOk = AnonStructD19c6c897d0b2f23;
pub type HostModelTypedByOk = NoneOrSomeType27;
pub type HostModelTypedByOkPayload = NoneOrSomeType27Payload;
pub type HostModelTypedByOkTag = NoneOrSomeType27Tag;
pub type HostModelTypedByOkSome = AnonStructD19c6c897d0b2f23;
pub type HostStateTransitionViewOk = AnonStruct5de797c596812777;
pub type HostStateTransitionViewOkCompleteness = AnonStruct4433c02c1fd6f4;
pub type HostStateTransitionViewOkCompletenessReasons = AnonStruct8ff265a5a21edc24;
pub type HostStateTransitionViewOkMachine = AnonStructDc524cbc8f8e5592;
pub type HostStateTransitionViewOkMachineSource = AnonStructDfa68fb5623390e9;
pub type HostStateTransitionViewOkMachineSourceRange = AnonStructF854c29f636ef8f8;
pub type HostStateTransitionViewOkNodes = AnonStructAb367e0f3bdf5730;
pub type HostStateTransitionViewOkNodesSource = AnonStructDfa68fb5623390e9;
pub type HostStateTransitionViewOkNodesSourceRange = AnonStructF854c29f636ef8f8;
pub type HostStateTransitionViewOkTransitions = AnonStructFfd4ba00196aaf2;
pub type HostStateTransitionViewOkTransitionsEffect = AnonStruct5bcf2c5d20eeb133;
pub type NoneOrSomeSomeRange = AnonStructF854c29f636ef8f8;
pub type HostStateTransitionViewOkTransitionsGuard = AnonStruct5bcf2c5d20eeb133;
pub type HostStateTransitionViewOkTransitionsSourceReference = AnonStructDfa68fb5623390e9;
pub type HostStateTransitionViewOkTransitionsSourceReferenceRange = AnonStructF854c29f636ef8f8;
pub type HostStateTransitionViewOkTransitionsTrigger = AnonStruct7d92a9ce2339c3f2;
pub type HostStateTransitionViewOkView = AnonStruct862498435f691a50;
pub type HostStateTransitionViewOkViewExposedMachine = AnonStructE5cb3809f97dc82d;
pub type HostStateTransitionViewOkViewSource = AnonStructDfa68fb5623390e9;
pub type HostStateTransitionViewOkViewSourceRange = AnonStructF854c29f636ef8f8;
pub type HostStateTransitionViewsOk = AnonStruct862498435f691a50;
pub type HostStateTransitionViewsOkExposedMachine = AnonStructE5cb3809f97dc82d;
pub type HostStateTransitionViewsOkSource = AnonStructDfa68fb5623390e9;
pub type HostStateTransitionViewsOkSourceRange = AnonStructF854c29f636ef8f8;
pub type GenerateForHostOk = AnonStruct3f5ac9322a5c230f;

// Generated Refcount Helpers

impl DebugOrErrorOrInfoOrWarning {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let _ = self;
        let _ = roc_host;
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let _ = self;
        let _ = amount;
    }
}

impl NoneOrSomeType3 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType3Tag::None => {},
            NoneOrSomeType3Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType3Tag::None => {},
            NoneOrSomeType3Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl AnonStruct5f154a81aee47322 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.model_digest.decref(roc_host); }
        unsafe { value.semantic_api_version.decref(roc_host); }
        unsafe { value.spec42_version.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.model_digest.incref(amount); }
        unsafe { value.semantic_api_version.incref(amount); }
        unsafe { value.spec42_version.incref(amount); }
    }
}

impl HostModelChildrenResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostModelChildrenResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostModelChildrenResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                {
                    let list = payload;
                    if list.has_one_ref() {
                        for item_ref in list.allocation_items() {
                            let item = *item_ref;
                                unsafe { item.decref(roc_host); }
                        }
                    }
                    unsafe { list.decref(roc_host); }
                }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostModelChildrenResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostModelChildrenResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl AnonStructD19c6c897d0b2f23 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.handle.decref(roc_host); }
        unsafe { value.metaclass.decref(roc_host); }
        unsafe { value.name.decref(roc_host); }
        unsafe { value.qualified_name.decref(roc_host); }
        unsafe { value.semantic_id.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.handle.incref(amount); }
        unsafe { value.metaclass.incref(amount); }
        unsafe { value.name.incref(amount); }
        unsafe { value.qualified_name.incref(amount); }
        unsafe { value.semantic_id.incref(amount); }
    }
}

impl NoneOrSomeType11 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType11Tag::None => {},
            NoneOrSomeType11Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType11Tag::None => {},
            NoneOrSomeType11Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl NoneOrSomeType12 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType12Tag::None => {},
            NoneOrSomeType12Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType12Tag::None => {},
            NoneOrSomeType12Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl HostModelElementResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostModelElementResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostModelElementResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostModelElementResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostModelElementResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl AnonStruct1ebccf7a1ff71691 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.declared_name.decref(roc_host); }
        unsafe { value.direction.decref(roc_host); }
        unsafe { value.documentation.decref(roc_host); }
        unsafe { value.effective_name.decref(roc_host); }
        unsafe { value.evaluated_value.decref(roc_host); }
        unsafe { value.multiplicity.decref(roc_host); }
        unsafe { value.owner.decref(roc_host); }
        unsafe { value.short_name.decref(roc_host); }
        unsafe { value.source_uri.decref(roc_host); }
        unsafe { value.summary.decref(roc_host); }
        unsafe { value.source_range.decref(roc_host); }
        unsafe { value.composite.decref(roc_host); }
        unsafe { value.ordered.decref(roc_host); }
        unsafe { value.reference.decref(roc_host); }
        unsafe { value.unique.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.declared_name.incref(amount); }
        unsafe { value.direction.incref(amount); }
        unsafe { value.documentation.incref(amount); }
        unsafe { value.effective_name.incref(amount); }
        unsafe { value.evaluated_value.incref(amount); }
        unsafe { value.multiplicity.incref(amount); }
        unsafe { value.owner.incref(amount); }
        unsafe { value.short_name.incref(amount); }
        unsafe { value.source_uri.incref(amount); }
        unsafe { value.summary.incref(amount); }
        unsafe { value.source_range.incref(amount); }
        unsafe { value.composite.incref(amount); }
        unsafe { value.ordered.incref(amount); }
        unsafe { value.reference.incref(amount); }
        unsafe { value.unique.incref(amount); }
    }
}

impl NoneOrSomeType17 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType17Tag::None => {},
            NoneOrSomeType17Tag::Some => {},
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType17Tag::None => {},
            NoneOrSomeType17Tag::Some => {},
        }
    }
}

impl NoneOrSomeType18 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType18Tag::None => {},
            NoneOrSomeType18Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType18Tag::None => {},
            NoneOrSomeType18Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl NoneOrSomeType19 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType19Tag::None => {},
            NoneOrSomeType19Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType19Tag::None => {},
            NoneOrSomeType19Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl AnonStructE7b668836e719b96 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.lower.decref(roc_host); }
        unsafe { value.upper.decref(roc_host); }
        unsafe { value.unique.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.lower.incref(amount); }
        unsafe { value.upper.incref(amount); }
        unsafe { value.unique.incref(amount); }
    }
}

impl NoneOrSomeType21 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType21Tag::None => {},
            NoneOrSomeType21Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType21Tag::None => {},
            NoneOrSomeType21Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl AnonStructF854c29f636ef8f8 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        let _ = value;
        let _ = roc_host;
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = value;
        let _ = amount;
    }
}

impl HostModelTypedByResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostModelTypedByResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostModelTypedByResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostModelTypedByResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostModelTypedByResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl NoneOrSomeType27 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType27Tag::None => {},
            NoneOrSomeType27Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType27Tag::None => {},
            NoneOrSomeType27Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl NoneOrSomeType30 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType30Tag::None => {},
            NoneOrSomeType30Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType30Tag::None => {},
            NoneOrSomeType30Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl HostModelRelationshipsResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostModelRelationshipsResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostModelRelationshipsResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                {
                    let list = payload;
                    if list.has_one_ref() {
                        for item_ref in list.allocation_items() {
                            let item = *item_ref;
                                unsafe { item.decref(roc_host); }
                        }
                    }
                    unsafe { list.decref(roc_host); }
                }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostModelRelationshipsResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostModelRelationshipsResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl AnonStruct682d6d8805bd09e9 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.kind.decref(roc_host); }
        unsafe { value.source.decref(roc_host); }
        unsafe { value.target.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.kind.incref(amount); }
        unsafe { value.source.incref(amount); }
        unsafe { value.target.incref(amount); }
    }
}

impl NoneOrSomeType37 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType37Tag::None => {},
            NoneOrSomeType37Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType37Tag::None => {},
            NoneOrSomeType37Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl HostStateTransitionViewsResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostStateTransitionViewsResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostStateTransitionViewsResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                {
                    let list = payload;
                    if list.has_one_ref() {
                        for item_ref in list.allocation_items() {
                            let item = *item_ref;
                                unsafe { item.decref(roc_host); }
                        }
                    }
                    unsafe { list.decref(roc_host); }
                }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostStateTransitionViewsResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostStateTransitionViewsResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl AnonStruct862498435f691a50 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.exposed_machine.decref(roc_host); }
        unsafe { value.handle.decref(roc_host); }
        unsafe { value.name.decref(roc_host); }
        unsafe { value.semantic_id.decref(roc_host); }
        unsafe { value.source.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.exposed_machine.incref(amount); }
        unsafe { value.handle.incref(amount); }
        unsafe { value.name.incref(amount); }
        unsafe { value.semantic_id.incref(amount); }
        unsafe { value.source.incref(amount); }
    }
}

impl AnonStructE5cb3809f97dc82d {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { value.semantic_id.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.semantic_id.incref(amount); }
    }
}

impl AnonStructDfa68fb5623390e9 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.uri.decref(roc_host); }
        unsafe { value.range.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.uri.incref(amount); }
        unsafe { value.range.incref(amount); }
    }
}

impl HostStateTransitionViewResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            HostStateTransitionViewResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            HostStateTransitionViewResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            HostStateTransitionViewResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            HostStateTransitionViewResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl AnonStruct5de797c596812777 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.completeness.decref(roc_host); }
        unsafe { value.machine.decref(roc_host); }
        unsafe { value.model_digest.decref(roc_host); }
        {
            let list = value.nodes;
            if list.has_one_ref() {
                for item_ref in list.allocation_items() {
                    let item = *item_ref;
                        unsafe { item.decref(roc_host); }
                }
            }
            unsafe { list.decref(roc_host); }
        }
        {
            let list = value.transitions;
            if list.has_one_ref() {
                for item_ref in list.allocation_items() {
                    let item = *item_ref;
                        unsafe { item.decref(roc_host); }
                }
            }
            unsafe { list.decref(roc_host); }
        }
        unsafe { value.view.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.completeness.incref(amount); }
        unsafe { value.machine.incref(amount); }
        unsafe { value.model_digest.incref(amount); }
        unsafe { value.nodes.incref(amount); }
        unsafe { value.transitions.incref(amount); }
        unsafe { value.view.incref(amount); }
    }
}

impl AnonStruct4433c02c1fd6f4 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        {
            let list = value.reasons;
            if list.has_one_ref() {
                for item_ref in list.allocation_items() {
                    let item = *item_ref;
                        unsafe { item.decref(roc_host); }
                }
            }
            unsafe { list.decref(roc_host); }
        }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.reasons.incref(amount); }
    }
}

impl AnonStruct8ff265a5a21edc24 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.code.decref(roc_host); }
        unsafe { value.message.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.code.incref(amount); }
        unsafe { value.message.incref(amount); }
    }
}

impl AnonStructDc524cbc8f8e5592 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { value.semantic_id.decref(roc_host); }
        unsafe { value.source.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.semantic_id.incref(amount); }
        unsafe { value.source.incref(amount); }
    }
}

impl AnonStructAb367e0f3bdf5730 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { value.semantic_id.decref(roc_host); }
        unsafe { value.source.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.semantic_id.incref(amount); }
        unsafe { value.source.incref(amount); }
    }
}

impl AnonStructFfd4ba00196aaf2 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.effect.decref(roc_host); }
        unsafe { value.guard.decref(roc_host); }
        unsafe { value.label.decref(roc_host); }
        unsafe { value.semantic_id.decref(roc_host); }
        unsafe { value.source.decref(roc_host); }
        unsafe { value.source_reference.decref(roc_host); }
        unsafe { value.target.decref(roc_host); }
        unsafe { value.trigger.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.effect.incref(amount); }
        unsafe { value.guard.incref(amount); }
        unsafe { value.label.incref(amount); }
        unsafe { value.semantic_id.incref(amount); }
        unsafe { value.source.incref(amount); }
        unsafe { value.source_reference.incref(amount); }
        unsafe { value.target.incref(amount); }
        unsafe { value.trigger.incref(amount); }
    }
}

impl AnonStruct5bcf2c5d20eeb133 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { value.source.decref(roc_host); }
        unsafe { value.unsupported.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.source.incref(amount); }
        unsafe { value.unsupported.incref(amount); }
    }
}

impl NoneOrSomeType63 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType63Tag::None => {},
            NoneOrSomeType63Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType63Tag::None => {},
            NoneOrSomeType63Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl NoneOrSomeType64 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType64Tag::None => {},
            NoneOrSomeType64Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType64Tag::None => {},
            NoneOrSomeType64Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl NoneOrSomeType65 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType65Tag::None => {},
            NoneOrSomeType65Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType65Tag::None => {},
            NoneOrSomeType65Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl AnonStruct7d92a9ce2339c3f2 {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.label.decref(roc_host); }
        unsafe { value.source.decref(roc_host); }
        unsafe { value.target.decref(roc_host); }
        unsafe { value.unsupported.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.label.incref(amount); }
        unsafe { value.source.incref(amount); }
        unsafe { value.target.incref(amount); }
        unsafe { value.unsupported.incref(amount); }
    }
}

impl NoneOrSomeType67 {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            NoneOrSomeType67Tag::None => {},
            NoneOrSomeType67Tag::Some => {
                let payload = unsafe { value.take_payload_some_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            NoneOrSomeType67Tag::None => {},
            NoneOrSomeType67Tag::Some => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_some_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl GenerateForHostResult {
    /// Recursively decrement Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted payload.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let mut value = self;
        let _ = roc_host;
        match value.tag {
            GenerateForHostResultTag::Err => {
                let payload = unsafe { value.take_payload_err_unchecked() };
                unsafe { payload.decref(roc_host); }
            },
            GenerateForHostResultTag::Ok => {
                let payload = unsafe { value.take_payload_ok_unchecked() };
                {
                    let list = payload;
                    if list.has_one_ref() {
                        for item_ref in list.allocation_items() {
                            let item = *item_ref;
                                unsafe { item.decref(roc_host); }
                        }
                    }
                    unsafe { list.decref(roc_host); }
                }
            },
        }
    }

    /// Increment Roc-owned payloads.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        let _ = amount;
        match value.tag {
            GenerateForHostResultTag::Err => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_err_unchecked()) };
                unsafe { payload.incref(amount); }
            },
            GenerateForHostResultTag::Ok => {
                let payload = unsafe { core::ptr::read(value.borrow_payload_ok_unchecked()) };
                unsafe { payload.incref(amount); }
            },
        }
    }
}

impl AnonStruct3f5ac9322a5c230f {
    /// Recursively decrement Roc-owned fields.
    ///
    /// # Safety
    /// `self` must own one live Roc reference for each refcounted field.
    pub unsafe fn decref(self, roc_host: &RocHost) {
        let value = self;
        unsafe { value.contents.decref(roc_host); }
        unsafe { value.file_path.decref(roc_host); }
    }

    /// Increment Roc-owned fields.
    ///
    /// # Safety
    /// `self` must point at live Roc allocations. The retained references must
    /// be balanced by later decrefs.
    pub unsafe fn incref(self, amount: isize) {
        let value = self;
        unsafe { value.contents.incref(amount); }
        unsafe { value.file_path.incref(amount); }
    }
}


// Runtime Symbols
//
// The host defines these linker symbols. Compiled Roc code calls them directly.

#[allow(improper_ctypes)]
unsafe extern "C" {
    pub fn roc_alloc(length: usize, alignment: usize) -> *mut c_void;
    pub fn roc_dealloc(ptr: *mut c_void, alignment: usize);
    pub fn roc_realloc(ptr: *mut c_void, new_length: usize, alignment: usize) -> *mut c_void;
    pub fn roc_dbg(bytes: *const u8, len: usize);
    pub fn roc_expect_failed(bytes: *const u8, len: usize);
    pub fn roc_crashed(bytes: *const u8, len: usize);
}

// Hosted Symbols
//
// The platform host must export these symbols with the exact direct C ABI signatures.
// Refcounted arguments are owned by the hosted function.

#[allow(improper_ctypes)]
unsafe extern "C" {
    /// Hosted symbol for HostDiagnostics.log!
    /// Roc signature: [Debug, Error, Info, Warning], Str => {}
    pub fn roc_diagnostics_log(arg0: DebugOrErrorOrInfoOrWarning, arg1: RocStr);

    /// Hosted symbol for HostDiagnostics.report!
    /// Roc signature: [Debug, Error, Info, Warning], Str, [None, Some(Str)] => {}
    pub fn roc_diagnostics_report(arg0: DebugOrErrorOrInfoOrWarning, arg1: RocStr, arg2: NoneOrSomeType3);

    /// Hosted symbol for HostModel.children!
    /// Roc signature: Str => Try(List({ handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str }), Str)
    pub fn roc_model_children(arg0: RocStr) -> HostModelChildrenResult;

    /// Hosted symbol for HostModel.effective_features!
    /// Roc signature: Str => Try(List({ handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str }), Str)
    pub fn roc_model_effective_features(arg0: RocStr) -> HostModelChildrenResult;

    /// Hosted symbol for HostModel.element!
    /// Roc signature: Str => Try({ abstract_flag : Bool, composite : [None, Some(Bool)], conjugated : Bool, constant : Bool, declared_name : [None, Some(Str)], definition : Bool, derived : Bool, direction : [None, Some(Str)], documentation : [None, Some(Str)], effective_name : [None, Some(Str)], end : Bool, evaluated_value : [None, Some(Str)], individual : Bool, multiplicity : [None, Some({ implied : Bool, lower : [None, Some(Str)], ordered : Bool, unique : [None, Some(Bool)], upper : [None, Some(Str)] })], ordered : [None, Some(Bool)], owner : [None, Some({ handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str })], reference : [None, Some(Bool)], short_name : [None, Some(Str)], source_range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, source_uri : Str, summary : { handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str }, unique : [None, Some(Bool)], variation : Bool }, Str)
    pub fn roc_model_element(arg0: RocStr) -> HostModelElementResult;

    /// Hosted symbol for HostModel.find!
    /// Roc signature: [None, Some(Str)] => Try(List({ handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str }), Str)
    pub fn roc_model_find(arg0: NoneOrSomeType12) -> HostModelChildrenResult;

    /// Hosted symbol for HostModel.info!
    /// Roc signature: {} => { model_digest : Str, semantic_api_version : Str, spec42_version : Str }
    pub fn roc_model_info() -> AnonStruct5f154a81aee47322;

    /// Hosted symbol for HostModel.relationships!
    /// Roc signature: Str => Try(List({ implied : Bool, kind : Str, source : { handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str }, target : { handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str } }), Str)
    pub fn roc_model_relationships(arg0: RocStr) -> HostModelRelationshipsResult;

    /// Hosted symbol for HostModel.roots!
    /// Roc signature: {} => Try(List({ handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str }), Str)
    pub fn roc_model_roots() -> HostModelChildrenResult;

    /// Hosted symbol for HostModel.typed_by!
    /// Roc signature: Str => Try([None, Some({ handle : Str, library_element : Bool, metaclass : Str, name : [None, Some(Str)], qualified_name : Str, semantic_id : Str })], Str)
    pub fn roc_model_typed_by(arg0: RocStr) -> HostModelTypedByResult;

    /// Hosted symbol for HostStateTransition.view!
    /// Roc signature: Str => Try({ completeness : { complete : Bool, reasons : List({ code : Str, message : Str }) }, machine : { label : Str, semantic_id : Str, source : { range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str } }, model_digest : Str, nodes : List({ kind : U8, label : Str, semantic_id : Str, source : { range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str } }), schema_version : U32, transitions : List({ effect : { kind : U8, label : [None, Some(Str)], source : [None, Some({ range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str })], unsupported : [None, Some({ code : Str, message : Str })] }, guard : { kind : U8, label : [None, Some(Str)], source : [None, Some({ range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str })], unsupported : [None, Some({ code : Str, message : Str })] }, implied : Bool, label : [None, Some(Str)], semantic_id : Str, source : Str, source_reference : { range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str }, target : Str, trigger : { kind : U8, label : [None, Some(Str)], source : [None, Some({ range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str })], target : [None, Some({ label : Str, semantic_id : Str })], unsupported : [None, Some({ code : Str, message : Str })] } }), view : { exposed_machine : { label : Str, semantic_id : Str }, handle : Str, name : Str, semantic_id : Str, source : { range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str } } }, Str)
    pub fn roc_state_transition_view(arg0: RocStr) -> HostStateTransitionViewResult;

    /// Hosted symbol for HostStateTransition.views!
    /// Roc signature: {} => Try(List({ exposed_machine : { label : Str, semantic_id : Str }, handle : Str, name : Str, semantic_id : Str, source : { range : { end_character : U32, end_line : U32, start_character : U32, start_line : U32 }, uri : Str } }), Str)
    pub fn roc_state_transition_views() -> HostStateTransitionViewsResult;

}

/// Default memory management functions for Roc platform helpers.
///
/// Memory layout: each allocation prepends size metadata so that dealloc/realloc
/// can recover the original allocation size because `roc_dealloc` receives no length.
#[cfg(not(no_roc_std_helpers))]
pub struct DefaultAllocators;

#[cfg(not(no_roc_std_helpers))]
impl DefaultAllocators {
    /// Allocate memory using the Rust global allocator.
    pub extern "C" fn roc_alloc(_roc_host: *mut RocHost, length: usize, alignment: usize) -> *mut c_void {
        unsafe {
            let min_alignment = alignment.max(core::mem::align_of::<usize>());
            let size_storage_bytes = min_alignment;
            let total_size = checked_add_usize(length, size_storage_bytes, "roc_alloc allocation size overflow");

            debug_assert!(min_alignment.is_power_of_two(), "alignment must be a power of two");
            let layout = Layout::from_size_align_unchecked(total_size, min_alignment);
            let base_ptr = std::alloc::alloc(layout);
            if base_ptr.is_null() {
                eprintln!("roc_alloc: out of memory");
                std::process::exit(1);
            }

            let size_ptr = base_ptr.add(size_storage_bytes).sub(core::mem::size_of::<usize>()) as *mut usize;
            *size_ptr = total_size;

            base_ptr.add(size_storage_bytes) as *mut c_void
        }
    }

    /// Free memory previously allocated by `roc_alloc`.
    pub extern "C" fn roc_dealloc(_roc_host: *mut RocHost, ptr: *mut c_void, alignment: usize) {
        unsafe {
            let min_alignment = alignment.max(core::mem::align_of::<usize>());
            let size_storage_bytes = min_alignment;

            let size_ptr = (ptr as *const u8).sub(core::mem::size_of::<usize>()) as *const usize;
            let total_size = *size_ptr;

            let base_ptr = (ptr as *mut u8).sub(size_storage_bytes);
            debug_assert!(min_alignment.is_power_of_two(), "alignment must be a power of two");
            let layout = Layout::from_size_align_unchecked(total_size, min_alignment);
            std::alloc::dealloc(base_ptr, layout);
        }
    }

    /// Reallocate memory, preserving existing user data.
    pub extern "C" fn roc_realloc(_roc_host: *mut RocHost, ptr: *mut c_void, new_length: usize, alignment: usize) -> *mut c_void {
        unsafe {
            let min_alignment = alignment.max(core::mem::align_of::<usize>());
            let size_storage_bytes = min_alignment;

            let old_size_ptr = (ptr as *const u8).sub(core::mem::size_of::<usize>()) as *const usize;
            let old_total_size = *old_size_ptr;
            let old_base_ptr = (ptr as *mut u8).sub(size_storage_bytes);

            let new_total_size = checked_add_usize(new_length, size_storage_bytes, "roc_realloc allocation size overflow");
            debug_assert!(min_alignment.is_power_of_two(), "alignment must be a power of two");
            let old_layout = Layout::from_size_align_unchecked(old_total_size, min_alignment);
            let new_base_ptr = std::alloc::realloc(old_base_ptr, old_layout, new_total_size);
            if new_base_ptr.is_null() {
                eprintln!("roc_realloc: out of memory");
                std::process::exit(1);
            }

            let new_user_ptr = new_base_ptr.add(size_storage_bytes);
            let new_size_ptr = new_user_ptr.sub(core::mem::size_of::<usize>()) as *mut usize;
            *new_size_ptr = new_total_size;
            new_user_ptr as *mut c_void
        }
    }
}

/// Default handlers for dbg, expect-failed, and crash.
#[cfg(not(no_roc_std_helpers))]
pub struct DefaultHandlers;

#[cfg(not(no_roc_std_helpers))]
impl DefaultHandlers {
    /// Print a `dbg` expression to stderr.
    pub extern "C" fn roc_dbg(_roc_host: *mut RocHost, bytes: *const u8, len: usize) {
        unsafe {
            let msg = core::slice::from_raw_parts(bytes, len);
            let msg = core::str::from_utf8_unchecked(msg);
            eprintln!("[ROC DBG] {}", msg);
        }
    }

    /// Print a failed `expect` to stderr.
    pub extern "C" fn roc_expect_failed(_roc_host: *mut RocHost, bytes: *const u8, len: usize) {
        unsafe {
            let msg = core::slice::from_raw_parts(bytes, len);
            let msg = core::str::from_utf8_unchecked(msg);
            eprintln!("[ROC EXPECT] {}", msg);
        }
    }

    /// Print a `crash` message to stderr and exit.
    pub extern "C" fn roc_crashed(_roc_host: *mut RocHost, bytes: *const u8, len: usize) {
        unsafe {
            let msg = core::slice::from_raw_parts(bytes, len);
            let msg = core::str::from_utf8_unchecked(msg);
            eprintln!("[ROC CRASHED] {}", msg);
            std::process::exit(1);
        }
    }
}

/// Create a host-internal helper context with default memory management and handlers.
///
/// This is only for helper functions in this generated file. It is not passed to
/// compiled Roc code, which uses the direct symbol ABI declared above.
#[cfg(not(no_roc_std_helpers))]
pub fn make_roc_host(env: *mut c_void) -> RocHost {
    RocHost {
        env,
        roc_alloc: DefaultAllocators::roc_alloc,
        roc_dealloc: DefaultAllocators::roc_dealloc,
        roc_realloc: DefaultAllocators::roc_realloc,
        roc_dbg: DefaultHandlers::roc_dbg,
        roc_expect_failed: DefaultHandlers::roc_expect_failed,
        roc_crashed: DefaultHandlers::roc_crashed,
    }
}

// Provided Symbols
//
// Roc exports these symbols from the app with their natural C ABI signatures.

#[allow(improper_ctypes)]
unsafe extern "C" {
    /// Entrypoint: generate_for_host!
    pub fn roc_generate(arg0: RocList<RocStr>) -> GenerateForHostResult;

}
